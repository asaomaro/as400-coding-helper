---
backlog: dds
kind: standing        # standing（定常ドメインキュー）| split（タスク分割由来・短命）
priority: 1           # DDS の視覚的確認と編集（charter の第 4 の柱）
---
# DDS（DSPF/PRTF）バックログ

画面・帳票の視覚的な確認と編集に関するキュー。対象は `src/core/dds/`・`src/dds/`・
`src/language/{dspf,prtf}Preview*.ts`。

`aidev-util-batch` が消化する対象リスト。各未チェック行 = 1件のタスク。

## 項目

- [x] **プレビューとエディタの使い分けを案内する** — 済（`20260827-dds-preview-vs-editor`）。
  調べたら**案内以前に到達できていなかった**——ビジュアルエディタは `customEditors` に
  しか無く（`priority: "option"`）、コマンドもメニュー項目も持たないため
  「エディターで開く…」を知る利用者にしか届いていなかった。
  `rpgClSupport.openDdsVisualEditor` を足してプレビューと**同じ右クリックメニューに並べ**、
  3 つの `title` に役割（読み取り / 編集）を書いた。使い分けは
  `docs/dds-editor-and-preview.md`。配線は `verify-contributes.mjs` が
  `resolveDdsType` と `customEditors[].selector` の**両方**と突き合わせる
  （導線を足す前は落ちることを確認済み）。実測: 単体 703 件（+3）。
  ~~README か~~ 本 PJ に README は無いので文書は `docs/` 直下に置いた。

- [x] **実機ゴールデン（DBCS の桁）** — 済（`20260827-dds-keyword-continuation` の research F4）。
  12 桁目に置いた定数の実機の画面での占有を実測し、`constantSegments` と一致することを確認した
  （`'顧客保守'` は SO+4×2+SI ＝ **10 桁**で、**最初の文字は開始桁の次**に出る）。
  半角混在・全角の途切れ・DBCS の継続も一致。

- [x] **実機ゴールデンとの突き合わせを入れる（描画全体）** — 済（`20260827-dds-render-golden`）。
  `test/golden/RENDER1.dspf`（生成物）を実機に `CRTDSPF` して 5250 に出し、画面を
  `RENDER1.screen.json` に採った。突き合わせは `test/unit/ddsRenderGolden.test.ts` で
  **実機に繋がずに**走る（定数の桁・入力欄の表示桁数・色と属性・条件で出ないもの）。
  ゴールデンは**実機の事実だけ**を持つ（`cells` / `fields`。モデルの語彙は入れない）。
  採り直しは `verify/capture-render-golden.mjs`。
  - **欠陥を 1 件見つけた**: `Y`（数字のみ）× 小数点あり × 入力可の欄は実機が
    **1 桁広く**返す（小数点の場所）。既存の `signPositions` は `S` の符号だけを見ており、
    小数点を取りこぼしていた。表示桁数の規則は原典に表が無いので**実機に判定させた**
    （`verify/probe-display-length.mjs`。20 通り）。
  - 落ちることを 2 通りで確認済み（`Y` の +1 を戻す → 2 件 / DBCS を 1 桁に数える → 10 件）。
  - 実測: 単体 750 件（+47）。

- [x] **CLI に DDS の操作を足す（`parse` / `render` / `patch`）** — 済（`20260827-dds-cli`）。
  `out/cli/dds.js`。`render --format text` は**桁の合った絵**を出す
  （DBCS の SO/SI も 1 桁として数え、`DSPATR(ND)` は `·` で「桁は占めるが文字が出ない」を示す）。
  `patch` は編集を検証して当てる（`--write` が無ければ書かない）。
  画面・帳票の両方に効き、帳票の用紙は `--page-rows` / `--page-columns` / `--overflow`。
  - ~~`validate`~~ は**作らなかった**。桁位置と配置の検査は **lint が同じ判定を出している**
    （`src/lint/rules/layout.ts:52` が `resolveDspfLayout` / `resolvePrtfLayout` を包む）。
    2 つ目の入口は「どちらが正か」を生む。使い方から `lint.js` へ案内している。
  - **穴が 1 つ出た**（下の項目）。CLI を通した瞬間に、GUI では到達しにくい経路が見えた。
  - 使い方は `docs/dds-editor-and-preview.md`。実測: 単体 771 件（+21）。

- [x] **`validateDdsEdits` に種別を渡す（1 行 1 桁・行送り）** — 済（`20260827-dds-edit-type-aware`）。
  **起票の前提が半分まちがっていた。** 実機に判定させたところ
  （`verify/probe-column-one.mjs`。IBM i 7.3）、通らないのは **1 行 1 桁だけ**だった:
  2 行 1 桁・1 行 2 桁・重なり・はみ出しは**すべて通る**。
  ~~原典は「最初の桁は属性文字のために予約されています」と禁じ~~ 原典の文は広く読めるが、
  **続く例は 1 行 1 桁**を指しており、実機は例のとおり。
  - 断るのは 1 行 1 桁だけ（帳票は属性文字が無いので対象外）。重なり・はみ出しは
    **拒否しない**——既存の判断（「直すために動かせる必要がある」）が正しかった。
  - **別の欠陥が出た**: `layout-column-one-reserved`（**既定 ON・error**）が行を見ずに
    報告しており、**実機で通るソース（5 行 1 桁など）を誤検出**していた。行も見るようにした。
  - 種別は**必須の引数**（`EditableDdsType`）。任意にすると渡し忘れが黙って検査を消す。
    必須にした瞬間に本番の呼び出し 4 か所がコンパイルエラーになり、洗い出しの手段になった。
  - `row-from-spacing` を帳票だけにした（画面ファイルに `SPACE` / `SKIP` は無い）。
  - 実測: 単体 839 件（+13）。

- [x] **エディタの GUI e2e を CI に載せる** — 済（`20260827-dds-e2e-on-ci`）。
  `.github/workflows/prompter-definitions.yml` の **`gui-e2e` ジョブ**が PR と main で回す。
  `verify` とは別ジョブ（並行）なので既存のジョブを遅くしない。
  ブラウザは `actions/cache` に載せ、`playwright-core` は**版を固定**して `--no-save`
  （浮動にするとキャッシュの鍵が毎回変わる）。
  **不安定なまま載せていない**——先に手元で **10 回連続緑**を確認した
  （109 件・1 回 25 秒。記録は `verify/stability-2026-08-27.txt`、測り直しは `e2e-stability.sh`）。
  `continue-on-error` は付けていない（落ちたら止まる）。
  ~~15 件~~ 起票時は 15 件だったが、いまは 109 件。

- [x] **表示トグルを足す（SO/SI の `{ }` 表示・属性バイト・グリッド・ズーム）** —
  `20260826-dds-display-toggles` で実装（PR は本文参照）。
  `src/dds/webview/ui.ts` のツールバーに切替を置き、SO/SI は**既に桁が空いている区切り**に
  `{` `}` を描く（表示の有無で項目の位置と幅が変わらないことを e2e で固定）。
  桁勘定（`SO/SI 2 + 全角 4 × 2 = 10 桁`）もプロパティに出る。
  - **ズームはボタンだけ**にして `Ctrl+=` を張らないことで、確定デザインの未解決 5
    （ホストのズームと二重に効く）を**回避**した。**解決ではない**——
    `docs/design/dds-designer/README.md` に扱いを記載。
  - ~~スナップ~~ は**作らない**判断（常にセルへ吸着しており、切替の相手が存在しない）。
  - ~~5250 配色~~ → 済（`20260827-dds-5250-colors`）。`COLOR` / `DSPATR` から実機の見え方
    （色・反転表示・下線・明滅・非表示）で描く。対応表は**原典から生成**し、
    **実機の画面と全 61 通りを突き合わせて**確認済み。
    `UL＋HI＋RI` が非表示になる（コンパイルも通り警告も出ない）ことを画面で示す。

- [x] **条件つきの `COLOR` / `DSPATR` を条件として扱う** — 済（`20260827-dds-conditional-attributes`）。
  `toLogicalUnits` がキーワードだけの行の条件付け欄を捨てていたので、`30 DSPATR(RI)` は
  **標識を倒しても反転表示のまま**だった。キーワード欄を**条件ごとの群**（`keywordGroups`）に
  分けて持ち、`applyIndicators` が見え方を解き直す。
  - `keywords`（全部の連結）の意味は**変えていない**——`readConstant` / `fieldWidth` /
    `readSpacing` / チップ表示 / `setKeywords` の 5 か所が読んでおり、引くと意味が動く。
    連結が一致することを 4 つのソースで固定した。
  - 原典「2 つ以上の COLOR キーワードが**有効になっている**場合には…最初のもの」に従い、
    **絞ってから最初を採る**（条件で最初の COLOR が落ちると次が効く）。
  - 未設定は**効かせる**（項目の表示と同じ倒し方。ずらすと「項目は残るのに色だけ消える」）。
  - 先行する条件行を**次の項目へ持ち越していた**誤りも直った。
  - プロパティに `条件つき` の行が出る（条件・キーワード・いま効くか）。
  - 実測: 単体 784 件（+13）・GUI e2e 109 件（うち条件つき属性 5 件）。

- [x] **条件つきの `EDTCDE` は幅に効かせていない** — **直す必要が無かった**
  （`20260827-dds-conditional-edtcde`）。実機で確かめたところ
  （IBM i 7.3。`verify/probe-conditionable-keywords.mjs`）:

  | キーワードを条件付ける | `CRTDSPF` |
  |---|---|
  | `DSPATR(RI)` / `COLOR(RED)` | **通る** |
  | `EDTCDE(J)`（`B` / `O` とも） | **通らない**（`CPF7311`） |
  | `EDTWRD(...)` / `CHECK(RB)` | **通らない** |

  ~~幅が変われば配置も重なりも変わる~~ **条件つき `EDTCDE` は有効なソースに存在しない**ので、
  連結した `keywords` から求めるいまの実装が正しい。
  `keywordGroups` があるぶん「解けるのに解いていない」と読めてしまうので、
  **直すべきでない理由を `ddsFieldWidth.ts` の注記に残した**。
  併せて `COLOR` / `DSPATR` が条件付けできることも確認（前 work の前提の裏取り）。

- [x] **条件付けできないキーワードを知らせる** — 済（`20260828-dds-keyword-conditionable`）。
  ~~ローカルの原典スナップショットは索引項目しか持っていないので、取得から~~
  **詳細ページは 287 件すでにあった**（`docs/origin/dds/detail/`）。決まり文句
  （「オプション標識は、このキーワードでは無効です」/「…使用することができます」）から
  `generate-dds-conditioning.mjs` が生成する（DSPF 153 件 / PRTF 64 件）。
  - **抽出の当たりは実機で錨を打つ**。`verify-dds-conditioning.mjs` が実機で確かめた
    5 件と件数の下限を見る。言い回しを取りこぼすように壊すと落ちることを確認済み。
  - **ひっかけ**: 付けられないキーワードは続けて「ただし…**フィールドの条件付け**を行う
    ことはできます」と書く。素朴に拾うと答えが反転する（いまの原典では「無効です」が
    先に出るので実害は無いが、順序に依存しない形にしてある）。
  - 原典が可否を書いていないもの（DSPF 17 / PRTF 1）は**咎めない**。
    既定 ON・error なので偽陽性の代償が大きい。
  - 指摘であって**編集は止めない**——キーワード行には複数のキーワードが並びうるので
    拒否の粒度が合わない。lint（既定 ON・error）が CI で止める。
  - 実測: 単体 857 件（+11）・verify 17 項目（+1）。

- [x] **条件の解決と標識パネル（C1 の左ペイン下段）** — 済（`20260826-dds-indicators`）。
  原典どおりの DNF（AND の組を OR で結ぶ）で解決し、標識を 3 値（未設定 / オン / オフ）で倒すと
  描画が変わる。**未設定は「決まらない」＝描く**ので、既定の見え方は変わらない
  （`applyIndicators(model, {})` が引数を**同一参照で**返す。`src/core/dds/dspfRenderModel.ts`）。
  一覧は**生の行**から集める——`toLogicalUnits` はキーワードだけの行の条件付け欄を捨てるため
  （`30 DSPATR(RI)` の `30` が落ちる。`src/core/dds/ddsConditioning.ts` `collectIndicators`）。
  実測: 単体 569 件（+42）・GUI e2e 55 件（うち標識 13 件）。

- [x] **条件標識の編集（付け外し）** — 済（`20260827-dds-condition-edit`）。
  プロパティの `条件` 欄で**短い形**（AND は空白 / OR はカンマ。例 `N50 01, 60`）を打つと、
  `writeBackCondition` が 7-16 桁に落とす。**桁は打たせない。**
  - **OR や 4 つ以上の AND では行が増える**（原典: 項目は「最後の (または唯一の) 標識と
    同じ行」）。宛先は代表行 1 本ではなく**区間**。空にすると代表行 1 本に戻り、
    元のソースと完全一致する。
  - 上限（1 つの条件に 9 個・条件は 9 つ）と `01-99` は**原典の数値をそのまま**。
  - 注記行が挟まる形は断る（区間で置き換えると注記が消える）。
  - **行がずれると選択が迷子になる**問題を e2e が捕まえた。行数の数え方を
    `conditionLineCount` の 1 か所に置き、書き出す本数との一致を検査で固定した。
  - 往復は 10 通りの形で確認（AND 9 個・OR 9 つの上限を含む）。
  - 実測: 単体 823 件（+39）・GUI e2e 117 件（うち条件編集 8 件）。

- [x] **キーワードだけの行の条件の編集** — 済（`20260827-dds-keyword-condition-edit`）。
  プロパティの「キーワード行」欄で、行ごとに条件を短い形で編集できる。
  書き戻しは項目の条件と**同じ経路**（`writeBackCondition`）で、新しい桁の規則は書いていない。
  - 宛先が違うので**別の編集の種類**（`setKeywordCondition`）にした。同じ `setCondition` で
    行番号の意味を変えると、どちらを指しているのか分からなくなる。
  - **条件が無いキーワード行も一覧に出す**（見出しを「条件つき」→「キーワード行」に変更）。
    条件つきだけを出すと**条件を新しく足す手段が無い**。
  - 代表行（項目の行）を宛先にしたら断る（`keyword-line-not-found`）。
  - 項目の行は動かない（キーワード行は代表行より後ろ）ので、選択の付け替えは**足していない**。
  - 実測: 単体 846 件（+7）・GUI e2e 122 件（+5）。

- [x] **画面サイズ条件名（`*DS3` 等）の編集** — 済（`20260828-dds-screen-size-condition`）。
  条件の入力欄で**先頭が `*` なら画面サイズ条件名**として読む（7 桁目はブランク・
  名前は 8 桁目から。読む側と対）。標識とは排他で、混ぜたら断る。
  ~~短い形に混ぜられない~~ 混ぜられないからこそ**同じ欄で受けられる**——
  入力の形が排他を自然に守る。
  編集の型は増やさず `screenSize?: string` を足した（`setAttributes` と同じ house style）。
  これで条件付け欄に入るものは**全部**編集できるようになった。
  実測: 単体 867 件（+10）・GUI e2e 125 件（+3）。
  - ⚠ ~~7 桁目はブランク・名前は 8 桁目から~~ **桁を誤っていた**。
    実機は 8 桁目を通さない（7 と 9 は通る）。`20260828-dds-screen-size-column` で
    9 桁目に直した（原典の例もその桁）。**自分の読み書きの間では往復していたので
    テストも e2e も通っていた**——桁の裏取りをしていなかった。

- [x] **使えない画面サイズ条件名を知らせる** — 済（`20260828-dds-undeclared-screen-size`）。
  ~~`DSPSIZ` に宣言されているか~~ **規則が違った。** 実機で 8 通り試して確定
  （2026-08-28 / IBM i 7.3）:

  | `DSPSIZ` | 条件名 | `CRTDSPF` |
  |---|---|---|
  | 無し / `(24 80)` | `*DS3` / `*DS4` | 通らない |
  | `(24 80 27 132)` | `*DS3`（**1 次**） | **通らない** |
  | `(24 80 27 132)` | `*DS4`（2 次） | 通る |
  | `(27 132 *WIDE 24 80 *NORMAL)` | `*WIDE`（**1 次**） | **通らない** |
  | 同上 | `*NORMAL`（2 次） | 通る |
  | 同上 | `*NOTDEC` | 通らない |

  8 通りすべてを「**条件名は 2 次画面サイズを指す**」という 1 つの規則が説明する
  （項目の行が 1 次の位置を与えるので、1 次を条件にするのは矛盾）。
  lint 規則 `layout-invalid-screen-size-condition`（既定 ON・error）。
  - **論理単位からは 1 行も見つからなかった**——条件名が書かれる行の多くは
    「条件名 ＋ 位置」だけの**位置の上書き行**で、`toLogicalUnits` が捨てている。
    `collectIndicators` と同じく生の行から探す。
  - 実測: 単体 899 件（+9）。

- [x] **画面サイズの「位置の上書き行」を配置に反映する** — 済（`20260828-dds-alternate-position`）。
  上書き行を直前の項目に付け、**2 次画面サイズの絵**を出せるようにした
  （デザイナのツールバーに `2 次画面`、CLI に `--screen-size secondary`）。
  - ~~「1 次と 2 次で位置が違う項目」をモデルが持てるようにする必要がある~~
    **持たせなかった**。`resolveDspfLayout` に「どちらのサイズで解くか」を渡し、
    **解決を 2 回まわす**。位置を 2 つ持たせると、読む側すべて（描画・重なり・
    はみ出し・一覧・プロパティ・編集）が「どちらの位置の話か」を意識することになる。
  - 「条件だけの行」と「上書き行」は**位置の有無**で見分ける（前置きは位置を持たない）。
  - **条件名が書ける行の形も実機で洗った**（9 通り）。上書き行だけが通り、
    定数・項目・様式・キーワードの行は通らない。
    **原典と食い違う**——原典は「キーワードの使用や…条件付ける」と書くが、
    実機は `DSPATR` / `COLOR` / `OVERLAY` のいずれも通さなかった。
  - **2 次の表示では動かせない**——位置を決めているのは上書き行なので、
    掴むと項目自身の行（＝1 次の位置）を黙って書き換えてしまう。
  - 実測: 単体 904 件（+4）・GUI e2e 137 件（+7）。

- [ ] **2 次画面サイズでの編集** — いまは 2 次の絵を見られるだけで動かせない。
  動かすには `move` の宛先を**位置の上書き行**にする必要がある
  （上書き行が無ければ作る）。出所: `20260828-dds-alternate-position` の `decisions.md` D3。

- [x] **キーワード欄を読める形にする** — 済（`20260826-dds-keyword-help`）。
  キーワード欄をチップに分け（引用符の中の空白・括弧で切らない）、選ぶと**原典の解説**
  （和名・使用レベル・構文・説明）が出る。解説は**ホストが `load` で渡す**
  （core はファイルを読まない。`src/core/dds/ddsKeywords.ts`）。
  `CF03` は原典の総称 `CFnn` に正規化して引く。**様式（レコード・レベル）も選べる**ようにした
  ——`OVERLAY` / `CF03` は様式宣言の行にしか無く、項目の一覧からは辿れなかった。
  実測: 単体 593 件（+23）・GUI e2e 66 件（うちキーワード 11 件）。

- [x] **キーワード欄の継続行を読めるようにする** — 済（`20260827-dds-keyword-continuation`）。
  継続の規則は**実機（IBM i 7.3）のコンパイラに判定させて**確定した
  （`-` は空白を挟まず 45 桁目ちょうどから / `+` は先頭の空白を捨てる /
  記号なしで引用符が開いていれば空白 1 つ）。それまでは継続元の行が
  **項目として認識されず、キャンバスから丸ごと消えていた**（実機で通る DSPF の定数 4 個が消失）。
  実機との突き合わせは `.aidev/works/20260827-dds-keyword-continuation/verify/` で再現できる。

- [x] **キーワード欄の編集（L3）と折り返しの書き出し** — 済（`20260827-dds-keyword-edit`）。
  チップの `✕` で外す / `＋` と原典の一覧から足す / 生テキストで引数を直す。
  36 桁に収まらなければ**切れ目で折り**、1 つのキーワードが超えるときだけ `-` で切る
  （`foldKeywordArea`）。継続にまたがる定数のリテラルも変えられるようになった
  （前 work の `keyword-continuation` の拒否は解けた）。
  往復は**原典の表 176 件 × 3 形**で総当たり。**実機でも折った結果の解決値が一致**する
  （`verify/verify-keyword-fold.mjs`。空白を含むリテラルの長さ 25 が保たれることを含む）。
  実測: 単体 633 件（+21）・GUI e2e 76 件（うち編集 10 件）。

- [x] **ファイル・レベルのキーワードをデザイナから読めるようにする** — 済
  （`20260828-dds-file-level-keywords`）。一覧の先頭に「ファイル」を置き、選ぶと
  チップと原典の解説が出る。CLI の `parse` にも `fileKeywords` として出る。
  - **`toLogicalUnits` は変えていない**。読み手（配置解決・一覧・編集）はどれも
    「単位＝置けるもの」を前提にしており、混ぜると置こうとする。**別の口**
    （`fileLevelKeywordLines`）から読む。
  - 行の分類は `classifyDdsLine` に切り出して**両方から使う**（写すと同じ行の扱いが
    2 通りになる）。
  - **読むだけ**（`✕` / `＋` / 生テキストの編集を出さない）。編集の宛先は論理単位なので、
    出しても `line-not-found` で断られる——効かない操作を出さない。
  - 一覧のクラス名を `record` / `item` と**共有した瞬間に壊れた**（項目を選ぶ側と
    様式を選ぶ側が拾った）。`file-level` / `file-keyword` に分けた。e2e が捕まえた。
  - 実測: 単体 881 件（+14）・GUI e2e 130 件（+5）。

- [x] **ファイル・レベルのキーワードを編集できるようにする** — 済（`20260828-dds-file-keyword-edit`）。
  **編集の種類は増やさず**、`setKeywords` の宛先を広げただけ（書き戻しは項目・様式と
  同じ経路なので、折り返しの規則が 2 か所に散らない）。宛先の引き当てだけを分け、
  論理単位から引けなければ**生の行から**ファイル・レベルの行を引く。
  - `＋`（候補から足す）は**出さない**——候補はキーワードの使用レベルで絞っており、
    ファイル・レベルの一覧をまだ持っていない。生テキストからは書ける。
  - 実測: 単体 908 件（+4）・GUI e2e 139 件（+2）。

- [ ] **ファイル・レベルのキーワードの候補（`＋`）を出す** — 使用レベルのデータは
  既に原典から生成しているので、`file` レベルで絞れば出せる。
  出所: `20260828-dds-file-keyword-edit` の `review.md`。

- [x] **`editedWidth` が表示装置ファイルの `Y` を弾く** — 済（`20260828-dds-edtcde-shift`）。
  ~~帳票と共用なので、帳票側も実機で確かめてから緩めること~~ 確かめた結果、
  **緩めてよいのは表示装置だけ**だった（2026-08-28 / IBM i 7.3）:

  | 種別 | `6Y 2O EDTCDE(J)` | `6S 2O EDTCDE(J)` |
  |---|---|---|
  | 表示装置 | **通る** | 通る |
  | 印刷装置 | **通らない**（`CPF7311`） | 通る |

  **原典どうしが食い違っていた**——印刷装置は「35 桁目に S かブランク」、
  表示装置は「編集が効力を持っている場合はシフトは Y」。種別で分けるのが正しい読み方。
  `editedWidth` / `fieldWidth` に種別を**必須引数**で渡す（渡し忘れると黙って
  印刷装置の規則が当たる）。表示装置の `6Y 2O EDTCDE(J)` は幅 9 になった（直す前は幅不明）。
  実測: 単体 890 件（+4）。

- [ ] **名前変更の参照追随**（`SFLCTL(NAME)` 等）— 現状は「追随しません」とプロパティに断っている。
  キーワードの解析（`parseKeywordEntries`）ができたので、引数の中の名前を見つける土台はできた。

- [ ] **行長の数え方を表示桁に揃えるか決める** — 編集の上限（100 桁）は lint と同じ
  **JS の文字数**で数えている。DBCS を含む行は実バイトではもっと長い。
  変えるなら `src/lint/rules/lineLength.ts` と**同時に**変える必要がある。
  出所: `20260826-dds-attribute-editing` の `decisions.md` D7。

- [x] **PRTF（帳票）の編集に広げる** — 済（`20260827-dds-prtf-editor`）。
  翻訳（`toRenderItem`）を種別に依らない形へ切り出し、`resolvePrtfLayout` から
  同じ `RenderModel` を作る。**行が行送り（SPACE / SKIP）で決まる項目は桁だけ動かせる**
  （`moveColumn`。行番号を書き込むと行送りが無効になるため）。
  帳票に無いもの（属性文字・5250 配色）は切替ごと出さない。
  **実機の印刷結果と行送りの解決が一致**することを確認済み
  （`verify/verify-prtf-rows.mjs`。`resolvePrtfLayout` はそれまで原典から起こしたままだった）。

- [ ] **帳票の強調（`HIGHLIGHT` / `UNDERLINE`）を描く** — 画面の 5250 配色にあたるもの。
  `DSPATR` は PRTF に無く、帳票は別のキーワードを使う。

- [ ] **複数ページの帳票を見る** — `resolvePrtfLayout` は 1 ページ分を返す。
  `SKIPB` で改ページが起きる帳票は 2 ページ目以降が見えない。

- [x] **帳票のプレビュー（CPI / LPI を紙の比率に反映）** — 済（`20260827-dds-prtf-preview`）。
  1 桁 = 1/CPI インチ・1 行 = 1/LPI インチで描き、用紙の大きさをインチで出す
  （原典の例「66 行 ÷ 6 LPI ＝ 11.0 インチ」で検算）。値の集合は**原典から生成**し、
  **実機に通る値と通らない値の両方を流して**一致を確認済み
  （`verify/verify-density-values.mjs`。`CPI(12)` / `LPI(7)` が弾かれることを含む）。

- [ ] **1 ページの中で LPI が変わる帳票を描く** — 原典は認めており
  （「6 LPI で 24 行、次に 8 LPI で 24 行」）、行ごとに高さが変わる。
  いまは 1 つの値で描き、**複数あることを知らせる**に留めている。
  「行 ＝ 一定の高さ」を前提にしているキャンバスの作りに関わる。

- [x] **キーワードの編集（L3）** — 済（`20260827-dds-keyword-edit`。上の項目と同一）。
  ~~いまの編集は位置と長さだけ~~ 属性（`20260826-dds-attribute-editing`）と
  キーワード（`20260827-dds-keyword-edit`）まで届いている。

- [x] **条件標識の編集（L2）** — 上の「条件標識の編集（付け外し）」と同じ 1 件。済。

- [ ] **単独起動を製品にするか決める** — `dev/` のハーネスは検証用で、ファイル操作も undo も
  最小限。独立したアプリとして出すのか、継ぎ目の検証手段に留めるのかを決める。
  出すなら保存経路・undo・エラー処理を作り込む必要がある。
