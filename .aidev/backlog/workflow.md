---
backlog: workflow
kind: standing        # 定常ドメインキュー（ワークフロー基盤の実装・確認タスク）
priority: 2           # cl(1) の次。設計書: docs/workflow/ibmi-dev-workflow.md
---
# IBM i 開発ワークフロー バックログ

設計書 `docs/workflow/ibmi-dev-workflow.md`（work `20260719-ibmi-dev-workflow`）から
起票した実装・確認タスク。各未チェック行 = 1 件のタスク。
**`(repo:as400-web-emulator)` 付きの行は実装先が別リポジトリ**であり、本 PJ の
`aidev-util-batch` では着手しない（追跡のみ。実装はあちらの backlog と合流させる）。

## P1: 自律ループ成立に必須

- [x] EVFEVENT の取得を実機確認する — 2026-07-19 pub400(7.5) で全段確認。
      **SQL は不可**（`RUNSQLSTM` は SQL0084 / PASE `db2` は権限なし）で
      `CPYTOSTMF` を使う。行・桁・msgId・重大度が取れることまで確認。
      コマンド列は skill `ibmi-remote` に、訂正は設計書 4.1 に反映
- [x] **RPGUnit の pub400 導入可否を確認する** — 済（`20260829-rpgunit-pub400-feasibility`）。
      **pub400 には入れられない。確定。** 副作用ゼロで確かめた（読み取り ＋ `CHKOBJ` のみ）。
      - pub400 の一般アカウント（`MARO`）は **特殊権限が 1 つも無い**（`SPECIAL_AUTHORITIES` が空）。
      - **標準経路が塞がっている**。RPGUnit は save file 配布で `RSTLIB` が要るが
        **`CPF9802`**（`RSTOBJ` も同じ）。
      - **`RPGUNIT` という名前のライブラリすら作れない**（`CRTLIB` も `CPF9802`）。
        自分のライブラリは `MARO1` / `MARO2` / `MAROB` の 3 つだけ。
      - **ビルド系（`CRTBNDRPG` / `CRTSRVPGM` / `CRTCMD` / `CRTMSGF` 等）は使える**ので
        「絶対に無理」ではないが、RPGUnit のビルドは**ライブラリ名 `RPGUNIT` を前提**に
        しており、別名で入れるには手直しが要る。**やるかどうかは労力の判断。**
      - **SR-OSAKA なら標準経路で入る**（`*ALLOBJ` ＋ `*SAVSYS` があり `RSTLIB` が通る）。
      - `RUCALLTST` の結果出力の採取は導入が前提なので**未着手**（下の項目に送った）。

- [x] **RPGUnit を SR-OSAKA に導入する** — 済（`20260829-rpgunit-sr-osaka`）。
      **iRPGUnit v4.0.3.r** を既定名 `RPGUNIT` で導入（31 オブジェクト）。SAVF は `ASAOLIB/RPGUNIT`。
      - **最新は使えない。** v6.0.2.r は `RSTLIB` こそ通る（`TGTRLS(V7R3M0)`）が、
        `RUCRTRPG` が `CRTRPGMOD … TGTCCSID(…)` を発行し、**7.3 の `CRTRPGMOD` に
        `TGTCCSID` が無い**（`CPD0043`）。**保存形式の互換とコマンドの互換は別。**
      - **`TGTRLS` はバイト列から読めない。** SAVF 内の `VxRyMz` は*オブジェクトの作成
        リリース*で、復元可否を決める `TGTRLS` ではない（v4: 文字列 `V7R3M0` / `TGTRLS` は
        `V7R1M0`）。**判定は `DSPSAVF` の `Release level`。**
      - **配布元は GitHub `tools-400/irpgunit`**（SourceForge は 2024-11-03 に移行）。
        ただし v4.0.3.r は SAVF 単体が無く、SourceForge の Update Site zip の
        `Server/RPGUNIT.SAVF`（4,994,880 B / `sha256:3309f68e…54ea0`）に同梱。
      - **`RSTLIB` だけでは終わらない場合がある。** v4 の `upload_savf.cmd` は
        `A_INSTALL` の作成・実行まで指示するが、**既定名なら不要**（原典の記述どおり、
        実際 `RSTLIB` だけで動いた）。v6 には `RPGUNIT1` が無いので v4 の手順は当たらない。
      - `RUCALLTST` の結果出力の形は下の項目に採取済み。
      出所: `20260829-rpgunit-pub400-feasibility`。

- [x] **RPGUnit をどこに入れるか決める** — 済。**SR-OSAKA に標準経路で入れる**（上で実施）。
      pub400 は不可と確定（`RSTLIB` に要る `*ALLOBJ`/`*SAVSYS` が無い）。
      条件（入手元とバージョンを記録・導入ライブラリーを分けて後から消せる形）は満たしている
      ——`ASAOLIB` には混ぜず `RPGUNIT` に分離したので `DLTLIB`/`CLRLIB` で丸ごと戻せる。
- [x] 実機操作レシピを skill 化する — 済。**大部分は起票時点で既にあった**
      （`.claude/skills/ibmi-remote/SKILL.md` 206 行に ssh / pub400 経由の
      メンバー送受信・コンパイル・エラー取得が揃っていた）。**閉じ忘れ。**
      足りなかった 2 点を `20260829-ibmi-remote-hostserver` で追記（+144 行 / 削除 0）:
      - **ライブラリー・リストが持ち越せない**こと（`ADDLIBLE` は次のコマンドに効かない）。
        知らないと `CPF5715` を桁の問題と誤診する（実際に `20260828-rpg3-fspec-reclen` で発生）。
      - **いま実際に使っている hostserver 経路**（`ts5250` を Node から直接）。
        接続の型・最小のレシピ・コンパイルリストの読み方・対照と片付け。
      **この gap は同じ日に害を出した**——`CRTRPGPGM` のスプール名を `QRPGLST` と
      思い込んで 0 件しか返らず、**でたらめな語まで「有効」に見えた**。
      ところが同じ skill の 4.5 節に「スプール名はメンバー名」と**既に書いてあった**。
- [x] **SQL ツールを MCP に配線する** (repo:**ts5250**) — 済（配線は起票より前に完了。閉じ忘れ）。
      **リポジトリは `as400-web-emulator` → `ts5250` に改名**されている（`/workspaces/ts5250`）。
      `host_sql` が `packages/server/src/host-server-tools.ts:144` にあり、
      `mcp-server.ts:14` の `registerHostServerTools` で MCP に出ている。
      列メタデータ・`maxRows` の打ち切り（`truncated`）・LOB のしきい値まで実装済み。
      - **ただし受け入れ条件の「対象スキーマ検証」は実装されていない。**
        代わりに `allowWrite: true` の明示を要求する設計で、**コードに理由が書いてある**——
        「これは安全の境界ではない。`host_command` から `RUNSQL` を撃てば同じことが
        できるので、SELECT 専用に縛っても書き込みは止まらない。意図を毎回述べさせるためのもの」。
        **設計書 6 章 #1 の前提（SQL ツール側のスキーマ検証で書き込みを縛る）が
        成り立たない**ことを示しているので、下の項目に送った。
      確認: `20260829-mcp-wiring-stocktake`。

- [ ] **書き込みの安全境界を接続プロファイルの権限に置く** — 決定（2026-08-29）:
      **境界は実機ユーザーの権限**。ts5250 の実装が指摘するとおり `host_command` から
      `RUNSQL` が撃てるので、**SQL ツール側のスキーマ検証は原理的に境界にならない**。
      `host_command` を含む全経路に一律に効く唯一の境界が権限。
      `allowWrite: true` の明示は**意図表明として残す**（多層防御。境界とは呼ばない）。
      作業: (1) 設計書 6 章 #1 を「SQL ツール側でスキーマ検証」から書き換える
      (2) 読み取り専用の接続プロファイルの作り方（実機ユーザーの権限設定）を手順に落とす。
      出所: `20260829-mcp-wiring-stocktake`。

## P2: 品質向上

- [x] lint core パッケージを作る（桁検査） — 2026-07-20 PR#101 でマージ。
      `src/lint/` (vscode 非依存) ＋ CLI(SARIF) ＋ 編集中の診断。判定の共有は
      `src/core/` に切り出した。**規則は 3 つだけ既定 ON**（`line-length` /
      `numeric-field` / `numeric-alignment`）。要件の 4 項目のうち「必須欄の未入力」と
      「定義済み値以外」は、実機コンパイル確認済み 1060 行への実測で 30 件の偽陽性が
      出たため既定 OFF で枠だけ用意した（下の follow-up 参照）
- [x] lint core にキーワード使用レベル検査を足す — 済（`20260828-dds-keyword-levels`）。
      `layout-keyword-wrong-level`（**既定 ON**）。DSPF / PRTF / PF の 3 種別に効く。
      - **実機がその形を通さない**ことを確かめた（`verify/probe-levels.mjs`。7 通り）:
        `DSPSIZ` を様式や項目に / `OVERLAY` をファイルや項目に /
        `COLOR` を様式やファイルに置くと**通らない**。原典のレベルと全件一致。
      - **原典がレベルを書いていないキーワードは咎めない**（表に入れない）。
        補完側の「判別できなかったものはどのレベルでも出す」と対
        ——出す側は広く、**咎める側は狭く**。
      - **先に偽陽性を数えてから既定を決めた**。検証済みサンプル 5 件で 0 件
        だったので既定 ON（既存の基準どおり）。偽陽性が出る検査は切られ、
        切られた検査は無いのと同じ。
      - 資源は**名前とレベルだけ**にした（13.7 KB）。`dspfLayout` は WebView にも
        束ねられるので、解説つきの `dds-keywords.json` を取り込まない。
      - 実測: 単体 1050 件（+12）・既定 ON の規則が 9 → 10。
- [x] **任意 CL 実行・IFS 書き込みを MCP に配線する** (repo:**ts5250**) — 済（閉じ忘れ）。
      `host_command`（`host-server-tools.ts:624`）/ `host_write_file`（`:774`）/
      `host_read_file`（`:745`）。確認: `20260829-mcp-wiring-stocktake`。
- [x] **既存スプール読み取りを MCP に配線する** (repo:**ts5250**) — 済（閉じ忘れ）。
      `host_list_spools`（`host-server-tools.ts:660`）/ `host_get_spool`（`:697`）。
      **実際にこの作業で使っている**——DDS の実機検証でコンパイル・リストを読むのに
      `readSpooledPages` 経路を回した（`20260829-dds-restricted-expand` ほか）。
      確認: `20260829-mcp-wiring-stocktake`。

  なお ts5250 の MCP には **`host_*` が 24 個**登録されている（上記のほか
  `host_call_program` / `host_dtaq_*` / `host_list_jobs` / `host_list_objects` /
  `host_sql_explain` / `host_upload_table` 等）。起票時の想定より広い。
- [x] レシピを CLI 化する（**RPGUnit の分**）— 済（`20260830-rpgunit-runner`）。
      `tools/run-rpgunit.mjs`（+458 行）。ローカルのソース 1 本で
      **転送 → ビルド → 実行 → 結果採取**まで通り、終了コードで判定できる
      （`0`=全合格 / `1`=テスト失敗 / `2`=道具の異常。ビルド失敗は 2 に分けた）。
      `--xml` で JUnit XML を保存でき CI のレポーターにそのまま載る。
      - **道具にした理由は再発の防止**。実機に繋ぐ使い捨てが `.aidev/works/` に 65 本
        （うちスプールを読むもの 12 本）あり、書き直すたびに同じ罠を踏んでいた。
        `SRCMBR` はプログラム名と揃える／`INLLIBL` ＋ `RPGUNIT/` 修飾／
        `INQMSGRPY(*DFT)`／既定 20 秒では足りない、を中に閉じ込めた。
      - **実機依存でも回帰が残せる形にした**。`--self-test` が引数解決と XML 要約を
        13 件確かめる（テストデータは実機が実際に出した XML）。
- [ ] レシピを CLI 化する（**残り**）— 転送・コンパイル・エラー取得の CLI 化と、
      npm パッケージとしての固定化。RPGUnit 分（上）は `tools/` の単体スクリプトで、
      パッケージにはしていない。
- [x] **テスト対象をバインドして業務ロジックをテストできるようにする** — 済
      （`20260831-rpgunit-bind-target`）。それまで実機で通した 7 本は**すべて自己完結**で
      （`iEqual(2:2)` / 0 除算 / `SYSDUMMY1`）、`BNDSRVPGM` を**一度も指定していなかった**。
      「RPGUnit が動くこと」は証明できていたが「業務プログラムをテストできること」は
      未証明だった。
      - `tools/run-rpgunit.mjs` に `--bnd`（繰り返し可）を追加。
      - **実例を `tools/example/` に置いた**（プロトタイプ ＋ 対象 ＋ テストの 3 本）。
      - **効いていることを壊して確かめた**。テストを 1 文字も触らず対象の
        `AMOUNT * (1 + RATE)` を `AMOUNT * RATE` にすると
        `Expected 110, but was 10.` で落ち、戻すと緑に復帰した。
        **通っている実例だけでは、バインドが効いていなくても緑になる。**
      - `BNDSRVPGM` の修飾は **`library/name`**（`QUAL` 2 段。空白区切りは 2 要素として
        読まれる）。原典 `RPGUNIT/QCMD,RUCRTRPG` で確定した。
      - 併せて道具の穴を 1 つ塞いだ——**`SBMJOB` の成否を見ていなかった**ので、
        投入自体が失敗したときに本当の理由が消えていた。
- [ ] AI によるテスト生成の方式を確立する — 設計書 4.2「AI にテストを書かせる場合の方針」を
      実行手順に落とす。受け入れ条件: (1) 新規は設計書をオラクルにする (2) 既存は特性化と
      明記させる (3) ミューテーションで検出力を検品 (4) `ORDER(*REVERSE)` で独立性を検品。
      **残るのは (1)(2) の手順化のみ。**
      - (3) **やり方は実証済み**（`20260831-rpgunit-bind-target`）。対象を壊して
        落ちることを確認する往復。手順としてはまだ書いていない。
      - (4) **道具になった**（`20260831-rpgunit-independence`）。
        `run-rpgunit.mjs --check-independence` が正順・逆順を走らせて合否を突き合わせる。
        **検査が発火することも確認済み**——わざと順序依存のテストを書いて終了コード 1 で捕まえた。
      - なお **V2 API（`assertEqual`）を使う**こと自体が (1) に効く。旧 `iEqual` は型で
        丸めるため誤った期待値を隠す（実例を実機で踏んだ。`20260831` の一連）。
- [x] 固定長（P 仕様書）で RPGUnit テストが書けることを実機確認する — 済
      （`20260829-rpgunit-sr-osaka`）。**書ける。** 原典の例はすべて `**free` だが、
      `**free` を 1 行も含まない H/P/D/C 仕様のソースから
      `/COPY RPGUNIT/QINCLUDE,TESTCASE`（775 行・**1 行目が `**free`**）を引いて
      コンパイルも実行も通った。
      - **対照つきで確かめた。** 最初の試行は本命も対照（`**free` 版）も落ちており、
        対照が落ちたことで「固定長が原因ではない」と分かった。真因は `TESTCASE` が
        **入れ子で非修飾の `/include qinclude,TEMPLATES`** を持つことで、`RPGUNIT` が
        `*LIBL` に要る（外側の `/COPY` を修飾しても内側には効かない）。
      - 結果: `RNS9305 … 00 highest severity` が両方で出て、`RUCALLTST` は
        `2 test cases, 2 assertions, 1 failure, 0 error.`（わざと落とした 1 本が
        `Expected 2, but was 3.` ＋ `(FIXTST2->FIXTST2:900)` で報告された）。
      - **`RUCALLTST` の結果出力の形**（後段が読む先。スプール名は `QSYSPRT` ではなく **`RPGUNIT`**）:
        最終行が集計（`FAILURE.` / `n test cases, n assertions, n failure, n error.`）、
        失敗は `<手続き名> - FAILURE` ＋ 期待値/実際値 ＋ `(pgm->module:SEQNBR)`。
        `OPTION(*SRCSTMT)` を付けていれば SEQNBR から元ソースに戻せる。
        失敗時は `CPF9897` の escape も投げる（EVFEVENT とは別経路）。
      - **実機の再現物**: `ASAOLIB/QUNITSRC`（`FIXTST`/`FREETST`/`FIXTST2`/`RUDRV2`）と
        `ASAOLIB/FIXTST2 *SRVPGM`。
- [x] **clPrompter との F4 衝突を確認する** — 済（`20260829-clprompter-f4-conflict`）。
      両者のマニフェストを直読して突き合わせた（`docs/research/code-for-ibmi.md` §3.1）。
      - **軸が違う**。向こうは `editorLangId`、本 PJ は `resourceExtname`。
        文字列を見比べても重なりは分からない。
      - **clPrompter は `contributes.languages` を持たない**ので、その `when` が真になるかは
        **他の拡張がその languageId を登録しているか**次第。
      - **`.clp` で衝突する。そしてその原因は本 PJ 側**——`.clp` → `cl` の言語登録が
        向こうの `editorLangId == cl` を真にしている。`.clle` と `.cmd` は環境次第。
        RPG と DDS は衝突しない。
      - **どちらが勝つかは制御できない**（VSCode は拡張どうしの優先度を規定しておらず、
        登録順で決まる）。回避は利用者の `keybindings.json`（3 パターンを記載）。
      - **本 PJ の挙動は変えていない**。`.clp` の F4 を降りるのは本 PJ の CL プロンプターを
        使えなくすることと同義で、選ぶのは利用者。`cl` の言語登録も外せない
        （コメントトグル・タブナビ・CL 診断の発火条件でもある）。
- [x] **lint の値集合を修復する** — 済（`20260829-dds-restricted-values`）。原典が有効値の一覧の
      **直後の「注」**で DBCS のデータ・タイプ(J/E/O/G)を足しており生成器が読めていない。
      表示装置 38 桁目は「ブランクまたは 0」で 1 文字の正規表現に合わず両方落ちている。
      実機が受けるのに原典に無い値もある(`CUSTMNT.dspf` の 38 桁 "O")。
      `generate-dds-prompter.mjs` の注記解析＋実機での裏取りが要る
      - **下調べ済み**（2026-08-28）。原典の生テキストで確かめたこと:
        - **物理/論理の 35 桁**（`FIELD-PF-ldata.html`）: 一覧は
          `P S B F A H L T Z 5`、**その直後の「注」**が `J`(専用) `E`(択一)
          `O`(混用) `G`(図形) を足す。**注は取れる**（ローカルにある）。
        - **表示装置の 38 桁**（`FIELD-DSPF-pos38.html`）: 先頭が
          **「ブランクまたは 0」**（ゼロ）で、残りが `I B H M P`。
          いまの定義は `I B H M P` だけで、**ブランクと 0 が落ちている**。
        - **表示装置の 35 桁は原典がローカルに無い**。親ページ
          （`FIELD-DSPF-rzakcmstdfdt.html`）は「表示装置ファイルの有効な項目」への
          リンクだけで、値の一覧は**子ページにある**。`sources.mjs` の manifest にも
          子ページの項目が無い。**取得（`fetch-origin.mjs` ＋ manifest 追加）が要る。**
      - **進め方の順序**: (1) 子ページを manifest に足して取得 →
        (2) 「注」の解析を生成器に足す → (3) 実機で値集合を裏取り
        （`CUSTMNT.dspf` の 38 桁 `O` が通るのはなぜか、`0` と `O` の両方が通るのかを含む）→
        (4) **値集合が完全だと言える欄にだけ `restricted: true`** を付ける →
        (5) 検証済みサンプルで偽陽性 0 件を確かめてから既定 ON。
      - **部分的に有効にしない**。値集合が欠けたまま `restricted` を立てると、
        正しいソースを弾く（この規則が既定 OFF になっている理由そのもの）。
      - **結果（2026-08-29）**: 読み落としを 3 つ直し、**原典の誤植 1 件**を実機で決着させた。
        JSON は手で直さず生成器を直している。単体 1121 → **1128**、verify 19 検査は緑。
        **後退を戻すと落ちることを 4 件で確認済み。**
        | 欄 | 前 | 後 |
        |---|---|---|
        | 表示装置 35 桁 | **選択肢なし（自由入力）** | ブランク X A N S Y W I D M F L T Z J E O G |
        | 物理/論理 35 桁 | P S B F A H L T Z 5 | ＋ J E O G |
        | 表示装置 38 桁 | I B H M P | **ブランク O** I B H M P |
        - **日本語版の原典が誤っていた。** ja「ブランクまたは 0」/ en「Blank or O」。
          実機は **0 を CPD7410（でたらめな値と同じ）で弾き、O は通す**。`ORIGIN_ERRATA` に
          根拠つきで置き換えを書いた。**消すのではなく置き換える**——消すと日本語版から
          正しい値 O が失われる（一度そうして気付いた）。
        - **弾かれた ＝ 無効な値ではない。** H / M / P は `CPD7443`「使用目的が H または P の
          場合には…」/ `CPD7436`「メッセージ・フィールドの場合には…」で、**認識されている**。
          本文を読まずに外していたら実機が受ける 3 値を落としていた。
        - 副産物: `fetch-origin.mjs` の manifest 併合の鍵が言語を見ておらず、
          **5 件が余計に畳まれていた**（保存先パスから作るよう修正）。

- [x] **`restricted-value` を有効にする** — 済（`20260829-dds-restricted-enable`）。**既定 ON**。
      **`restricted` はまだどこにも立てていない。** 値集合が「完全だ」と言えるのは
      **実機で全値を判定した表示装置 38 桁だけ**で、残り 4 欄は原典を読んだだけ。
      | 欄 | 根拠 | `restricted` |
      |---|---|---|
      | 表示装置 38 桁 | 原典(en) ＋ **実機で全値を判定** | 立てられる |
      | 表示装置 35 桁 / 物理論理 35・38 桁 / 印刷装置 35 桁 | 原典のみ。**実機未確認** | 立てられない |
      **手順は確立済み**（`20260829-dds-restricted-values/verify/probe-pos38.mjs` を写す）:
      最小の DDS を作り、値を 1 つずつ差し替えて `CRTDSPF`、**対照を先頭と末尾に置き**、
      リストのメッセージ番号と**本文**で「値が無効」と「文脈違い」を読み分ける。
      そのうえで (1) 完全と言える欄にだけ `restricted: true` →
      (2) 検証済みサンプル 9 件で偽陽性 0 件を確かめてから既定 ON。
      **部分的に有効にしない**（値集合が欠けたまま立てると正しいソースを弾く）。
      (needs: lint core パッケージを作る（桁検査）)

      **確かめた欄だけを見る。** `restricted: true` が付くのは**実機で全空間
      （1 文字なら 37 通り）を試して原典と一致した欄だけ**で、いまは**表示装置 38 桁の 1 件**。
      検証済みサンプル 11 件で**偽陽性 0 件**を確かめてから ON にした。単体 1128 → **1134**。
      - **1 文字の欄は空間が 37 通りなので網羅できる。** 37 項目を 1 ソースに並べて 1 回で流す。
      - **一括の読み取りは一度まちがった。** リストが「ソース → 展開後ソース」の 2 部構成で
        後半が前半を上書きし、**37 文字中 25 文字が有効**という結果になった。
        同じ日の単独判定と食い違って気付いた。**最初に現れた方だけを採る。**
      - **「指摘なし ＝ 有効」ではない。** 原典に無いのに指摘が出なかった値は単独で確認した
        （`V` は作成できない＝無効 / 印刷装置 35 桁の `G` `O` は**作成できる＝原典が不足**）。
      - **付けなかった欄**: 印刷装置 35 桁（原典に無い `G` `O` を実機が受ける）/
        表示装置 35 桁（未確定）/ 物理論理 35・38 桁（**対照が落ちた**＝雛形の誤り）/
        17 桁（値を変えると行の種類が変わるので 1 文字の判定にならない）。
      - 副次的に、確かめていない欄が `restricted:false` になったことで
        **画面が候補つきの自由入力になり、`G` / `O` を書けるようになった**。

- [x] **残りの DDS 欄を実機で確かめて `restricted` を広げる** — 済（`20260829-dds-restricted-expand`）。
      **7 欄中 6 欄が実機と完全一致**し、`restricted: true` は 1 欄 → 7 欄になった。
      - **起票時の見立ては 3 つとも誤っていた**（どれも原典や実機ではなく、
        こちら側の道具の欠陥だった）:
        - 「物理/論理は雛形が誤り（対照 `A` が落ちた）」→ **雛形は正しく、コマンドが誤り**。
          **`CRTPF` に `REPLACE` は無い**（`CPD0043`）ので DDS が一度も評価されていなかった。
        - 「表示装置 35 桁は一括の読み取りが当てにならない」→ **当てにならないのは読み取り**。
          指摘行は直前のソース行に属するが**ページ境界で見出しが割り込む**。
          項目名で grep していたため `V` の指摘だけが消えていた。
        - 「印刷装置 35 桁は原典に `G` `O` が無い。errata が要る」→ **原典にある**
          （値の表の直後の注）。生成器の正規表現が「注:」の直後に「データ・タイプ」で
          始まることを要求しており、値から始まる印刷装置の注に当たっていなかった。
      - **17 桁は判定できた**。無効値は `CPD7410` を出し、印がちょうど 17 桁目を指す。
        一括で無効を確定し、印が付かなかった値だけ単独で確かめる 2 段構え。
      - **38 桁は文脈で値集合が違う**（原典も分けて書いている）。物理 `_B` /
        単純論理 `_BI` / 結合論理 `_IN` で、**1 つの定義が両方を担うので和が正**。
        `CRTPF` だけで測ると「定義が誤り」と誤診するところだった。
      - 残った 1 欄（**印刷装置 17 桁**）は実機が `H` を受けるのに原典が日英とも
        `R`／ブランクだけ。`H` の意味が分からないので `false` のまま据え置いた。
      - 実測: 単体 1145 件（+11）・e2e 69 件（+7）・サンプル 11 件で指摘 0。

- [x] **印刷装置 38 桁（使用目的）の選択肢が空** — 済（`20260829-dds-prtf-usage-values`）。
      原典の**3 つ目の並べ方＝箇条書き**（`<li><samp>O</samp> またはブランク: 出力専用</li>`）を
      読めるようにした。定義リストでも表でもないため落ちていた。
      - 巻き込みは**原典の全件走査で確認**——この形を持つのは日英 2 ページのみ。
        再生成の差分も `DDS-PRTF.json` だけで、他の欄は 1 文字も動いていない。
      - 実機（`CRTPRTF`）の全 37 通りは**位置あり／なしとも受理が ブランク `O` `P`**。
        原典と完全一致したので `restricted: true`（DDS の印は 7 → **8 欄**）。
        位置の両方を流したのは、原典が `P` について「位置は無効です」と書くため。
      - 副次的に**ラベルの先頭コロンを落とした**（箇条書きは意味の側にコロンを置くので、
        素直に採ると `P（: プログラム…）` になる）。
      - 実測: 単体 1148 件（+3）・e2e 71 件（+2）・サンプル 11 件で指摘 0。
- [x] RPG III の桁属性を実機で確定する — 済（`20260828-rpg3-numeric-columns`）。
      **C 仕様の長さ(49-51)・小数(52)、I 仕様の開始(44-47)・終了(48-51)・小数(52)**に
      `numericOnly` を付けた。実機（IBM i 7.3 / `CRTRPGPGM`）の判定:
      **英字は通らず、左詰めも通らない**。
      - **確かめた欄だけ**に付けた。F / O / E / L 仕様は名前から見て数値らしいが
        確かめていないので触っていない——原典が無いので、確かめていない欄は根拠がゼロ。
      - **起票の範囲だけでは届かなかった。** `numeric-alignment` が
        「行末で欄が切れている場合は右寄せか判定できない」として飛ばしており、
        `numericOnly` を足しても実サンプルの誤りが出なかった。固定長のレコードは
        空白で埋まるので「行が短い」は「残りが空白」＝左詰めで、**実機はその形を
        通さない**。思い込みを実機が正した。
      - **`RPG3SAMP.rpg` も壊れていた**（長さが 49 桁・小数なし）。直した形が通ることを
        対照つきで確認済み。これで `docs/src/` の RPG サンプル 3 件がすべて実機の桁に揃った。
      - **対照づくりで 3 回つまずいた**（`SETON` の書き忘れ / 数値の結果フィールドに
        小数が要る / I 仕様のレコード識別標識の桁）。いずれも**桁の話ではない失敗**で、
        対照が無ければ実機の規則として誤って記録していた。
      - 実測: 単体 1060 件（+10）・サンプル 9 件で指摘ゼロ。

- [x] RPG III の残りの数値欄を実機で確定する — 済（`20260828-rpg3-numeric-fields`、PR#135）。
      対照つきで 50 件ほど流し、**付ける欄と付けない欄**を分けた。
      - **数字だけ・右寄せ**（`numericOnly` を付けた 9 欄）: F `RECADDRLEN`(29-30) /
        `KEYSTART`(35-38)、O `SPACEBEFORE`(17) / `SPACEAFTER`(18) / `ENDPOS`(40-43)、
        E `ENTPERREC`(33-35) / `ENTPERTAB`(36-39) / `ENTLEN`(40-42) / `ENTLEN2`(52-54)、
        L `LINE1`(15-17) / `LINE2`(20-22)。スペース欄は `0`-`3` のみ（`4` は通らない）。
      - **数字だけではない**: O `SKIPBEFORE`(19-20) / `SKIPAFTER`(21-22)。実機は
        `01`-`99` / `A0`-`A9` / `B0`-`B2` / 空白を受ける（`QRG6016`）。**`A0` が通ることを実測**。
        `numericOnly` を付けると実機の通す値を弾くので付けていない
        （付けたら落ちるテストを `rpg3NumericColumns.test.ts` に置いた）。
      - **`BLOCKLEN`(20-23) は数値欄ではなかった。** IBM i は F 仕様の
        20-23 / 47-52 / 60-65 / 67-70 / 73-74 を**空白でなければならない**とする（`QRG2016`）。
        `SYMDEVICE`(47-52) も同じ。既存ソースを読むため欄は残し、help で警告している。
      - **副産物 3 件**（いずれも実機の判定つき）:
        - **E / L 仕様が消費経路に無かった**。`classifySpec` の switch に `E` / `L` が無く、
          定義 JSON はあるのに F4 も lint も**一度も届いていなかった**。足した。
        - **53 桁目は「ラベル」ではなく「継続」**（`QRG2067`。継続行が通ることも確認）。
          `LABELS` → `CONTINUATION` に改めた。
        - **L 仕様は 2 組しか使えない**（`QRG3049`。25-74 桁は空白）。`LINE3`〜`CHANNEL12`
          の 20 欄は実機では書けない。
      - 実測: 単体 1084 件（+24）・GUI e2e 182 件・verify 19 件・サンプル 9 件で指摘ゼロ。

- [x] **`RPG3SAMP.rpg` の F / I / O 仕様の桁ずれを直す** — 済
      （`20260828-rpg3-numeric-fields`、PR#135）。5 行（2/3/4/12/13）を定義どおりの桁に直した。
      ~~直した形も通らなかった。外部記述ファイルの F 仕様の書き方がまだ当たっていない~~
      **前回の切り分けが誤っていた。** 原因は F 仕様の書き方ではなく `*LIBL` で、
      `CPF5715 File CUSTMAS in library *LIBL not found` が出ていた。外部記述ファイルは
      コンパイル時に解決されるが、**コンパイル・ジョブは毎回別なので `ADDLIBLE` が効かない**。
      `*LIBL` に載る `QGPL` に置いたらリストに
      `EXTERNAL FORMAT CUSTREC RPG NAME CUSTREC` が出て、**F 仕様の桁は最初から合っていた**。
      - **`numericOnly` を足したら桁ずれが lint に見えた。** ~~数値欄を増やしても捕まえられない~~
        ずれた `OF`（オーバーフロー標識）の `F` が `KEYSTART`(35-38) に落ちるため、
        `numeric-field` が 3 行目を指摘する。起票時の見立ては `RECLEN` だけを見たもので誤り。
      - **教訓: コンパイル・リストはスプールから読める。**
        `QSYS2.OUTPUT_QUEUE_ENTRIES_BASIC` で番号を引き `host_get_spool` で本文が取れる。
        `CommandConnection` が返すのは `QRG0008 Severity level NN` までで、
        **欄ごとの理由はリストにしかない**。二分探索で切り分ける前にリストを読むこと。

- [ ] **`RPG3SAMP.rpg` をコンパイル対象外と明記する** — 決定（2026-08-29）:
      **桁の見本に徹する**（実機で通る形にはしない）。このファイルは `docs/src/` の
      手動チェックリスト用フィクスチャで VSIX には載らず、桁の正しさは
      `20260828-rpg-sample-columns` で実機照合済み。通す作業は「この例で何を見せるか」を
      変える別の成果物になる。残る 2 件は**桁ではなく中身**の話（対象外とする根拠として残す）:
      - `QRG4118『Externally-described-file name is invalid as Record-Format-Name』` —
        外部記述の I 仕様には**ファイル名ではなくレコード様式名**を書く。
        `ICUSTREC` ＋ `NS` ＋ 標識 01 で試したが**対照が落ちた**ので通る形は未確定。
      - `QRG7023『The Compiler cannot determine how program can terminate』` —
        `IF`（全手順）で `SETON LR` が無く、RPG サイクルが終われない。
      直すと「この例で何を見せるか」が変わる（終了処理を足すか、`IP` にするか）。
      作業: 桁の見本でありコンパイルは通らないことを `docs/src/CHECKLIST.md` に明記する。

- [x] **F 仕様の継続行（54-59 選択 / 60-65 記入）を定義に足す** — 済（`20260828-rpg3-fspec-continuation-options`）。
      53 桁目が継続欄であることは実機で確かめた（`QRG2067`。`K` ＋ `INFDS` ＋
      データ構造名の継続行がコンパイルを通る）。いまの定義には `CONTINUATION`(53) しか無く、
      **選択と記入の欄が無い**ので継続行を書けない。

      **手法だけ先に確立した**（2026-08-28、`20260828-rpg3-fspec-continuation`）。
      語の一覧は**出していない**——誤った一覧を出す方が、出さないより害が大きいため。
      - **実機は有効な集合を並べてくれない。** `QRG6016`（スキップ欄）や `QRG2016`
        （空白必須の桁）は本文に集合を書いていたが、選択欄の `QRG2023` は並べない。
        `ZZZZZZ` を流して確認済み。
      - **候補は PJ 内に原典由来のものがある。** ILE の F 仕様キーワード 45 件
        （`resources/completion/rpg-completion.json` の `keywords["F-SPEC"]`）。
        **選択欄は 6 桁なので 7 文字以上は桁で機械的に外れ**、残り 30 件。
        ただし **RPG III にしか無い語は取りこぼす**（RPG III の命令コードと同じ限界）。
      - **成否だけで判別してはいけない。** `GENLVL(20)` ＋ 作成の成否で流したら
        通ったのは `INFDS` だけになったが、`INFSR` のリストに出ていたのは
        `QRG2075『The Entry (positions 60-65) … is invalid with the specified option』`で、
        **`QRG2023` ではない**。つまり `INFSR` は**有効な語**で、記入が空だっただけ。
        **この設計で出した「受けない 29 件」は結論として使えない。**
      - **正しい判別はメッセージの種類**（どちらも重大度 30 なのでリストを読む必要がある）:
        `QRG2023` = 語が無効 / `QRG2075` = 語は有効・記入が違う。
      - **次に組むときは** 語ごとに記入の中身が違う（`INFDS` はデータ構造名、
        `INFSR` は副ルーチン名…）ので**選択ごとの仕掛け**が要る。30 件ぶん要るので
        **バッチの尻ではなく単独の work で扱う**。リストを 60 回読むのは重いので、
        **`CPYSPLF` でリストをデータベース・ファイルに落として SQL で引く**のが軽い。
      - 土台はある: `20260828-rpg3-fspec-continuation/verify/probe-option.mjs` の C1。

      **結果**: `CONTOPT`(54-59) / `CONTENTRY`(60-65) を `F-SPEC.json` に追加。
      53 桁目が `K` のときだけ出す（`dependsOn`）。**コードは 1 行も変えていない。**
      単体テスト 1110 → **1118**、`npm run verify` の 19 検査は緑。
      **後退を戻すと落ちることを 4 件で確認済み。**

      **実機の判定（50 語 ＋ 対照 8 件。対照は 2 巡とも 4/4）**:
      有効 15 件 = `INFDS` `INFSR` `RECNO` `PLIST` `PASS` `PRTCTL` `SLN` `RENAME`
      `SFILE` `IGNORE` `SAVDS` `IND` `NUM` `ID` `COMIT`。無効 35 件。
      生データは `.aidev/works/20260828-rpg3-fspec-continuation-options/verify/`。

      **起票時の見立てとの違い**:
      - ~~語ごとに記入の中身が違うので選択ごとの仕掛けが要る。30 件ぶん要る~~
        → **要らなかった。** 判別を「作成の成否」ではなく**メッセージ番号**にすれば、
        記入は全件共通のダミーでよい（`QRG2023` = 語が無効 / それ以外 = 語は有効）。
      - ~~`CPYSPLF` でデータベース・ファイルに落として SQL で引くのが軽い~~
        → **不要だった。** `NetPrintConnection.readSpooledPages` をスクリプト内で呼べば
        リストを直接読める。**ただしスプール名は `QRPGLST` ではなくプログラム名**
        （ここを取り違えて全件「有効」に見え、対照で気付いた）。
      - **候補の出所（ILE のキーワード）は 15 件中 5 件を取りこぼしていた。**
        2 巡目で `SAVDS` `IND` `NUM` `ID` `COMIT` が見つかった。
        よって**選択肢で縛らず** `inputType:"text"` にし、確かめた語は help に置いた。

- [x] CI の「再生成しても差分が出ないこと」を直す — **既に解消していた**（2026-08-27 確認）。
      ~~PR#95 以降 main がずっと赤い~~ 起票後に別の作業で直っており、
      `prompter-definitions.yml` の再生成ステップは `generate-cdml-rules.mjs` を
      呼んでいる（「これを飛ばすと…常に失敗する」と注記つき）。
      手元で 16 本の生成器を全部回して**差分ゼロ**、直近の CI も 6 連続で success。
      **起票を閉じ忘れていた例**——backlog は遅れるので、着手前に現物で裏を取ること
      （AGENTS.md / `aidev-00-start`）。
- [x] `docs/src/` の未検証サンプルを実機で確かめる — 済（`20260828-rpg-sample-columns`）。
      **真陽性だった。サンプルの方を直した。**
      - 実機（IBM i 7.3 / `CRTBNDRPG`）の判定: 原典の桁（長さ 33-39 右寄せ / 型 40 /
        小数 41-42）は**通る**、サンプルの形（長さ 32 / 型 33 / 小数 35）は**通らない**。
        実物 2 行をそのまま流しても通らない。
      - 誤りは **DS の下位フィールドだけ**（EMPMNT01 に 7 行・SLSENT01 に 11 行）。
        独立した `S` 定義は元から正しい。直した形が通ることを対照つきで確認済み。
      - **残った 1 件は偽陽性**だった。`DOU` の行に固定欄の桁を当てていた——
        `DEFAULT_C_NEW_OPCODES` が**手で並べた 10 件**で `DOU` が抜けていた。
        原典から生成した補完データの `fixedForm.columns` に「拡張演算項目 2」と
        書かれた **17 件**から採るようにした（＋ 演算項目を採らない 5 件）。
      - **既存テスト 4 件がサンプルの壊れに寄りかかっていた**（`EMPMNT01.rpgle` を
        「桁ずれのある材料」として使っていた）。壊れた材料をテストの中へ移した。
      - `CHECKLIST.md` の「すべてコンパイル確認済み」をやめ、**何が確認済みで
        何がそうでないか**を書いた。CI の lint 対象に 3 件を戻した。
      - 実測: 単体 1038 件（+5）・lint 対象 9 件で指摘ゼロ。
- [x] `positionResolver` に RPG 注記行のガードを足す — 済（`20260828-rpg-comment-line-guard`）。
      **足す側ではなく分類器（`rpgSpec.ts`）に置き、ルーラーの写しを外した**
      ——写しが 2 つになると 3 つ目が抜ける（もともとそれが原因）。
      - `classifyWithState` と **`absorb` の 2 か所**に要る。`absorb` は分類の結果に
        関わらず毎行呼ばれ、**注記を 1 行挟むと続くフィールド行が直前のレコード様式を
        見失う**（プログラム記述が外部記述になる）。
      - 最初は「注記行の 7-16 桁がファイル名として登録される」と書いたが、
        **その形は起こりえない**（注記行の 7-16 桁は必ず `*` で始まる）。
        **ガードを外してもテストが通った**ので気付き、実際に効く経路に書き直した。
      - 実測: 単体 1033 件（+5）。

## P3: 将来

- [x] **CI を整える（本 PJ 側）** — 済（`20260829-ci-regen-coverage`）。
      **lint core の CI 化は起票より前に既に済んでいた**（閉じ忘れ）:
      `verify-lint-core.mjs`（境界）・桁位置 lint（サンプルで指摘ゼロ）・単体テスト・
      統合・GUI e2e が `prompter-definitions.yml` で回っている。
      残っていたのは**再生成チェックの取りこぼし**で、そちらを直した:
      - **差分検査が 3 つの出力先を列挙**しており（`prompter/cl` / `navigation` / `completion`）、
        **`prompter/dds` `prompter/cmd` `prompter/rpg` が漏れていた**。
        つまり **DDS の定義 JSON は手で直しても CI が気付かない**状態だった。
        列挙をやめて `vscode-extension/resources` 全体を見るようにした。
      - 生成器も 2 つ抜けていた（`generate-dds-columns.mjs --lang=en` /
        `generate-rpg-io-definitions.mjs`）。後者は一度も CI で回っていなかった。
      - 手編集をコミットすると落ちることを確認済み。既存のドリフトはゼロ。

- [ ] **ts5250 のオフライン回帰を CI で** (repo:**ts5250**) — 着手済み・**方針は決着**（下記）。
      **リポジトリは改名されており `/workspaces/ts5250` にある**（以前「ローカルに無い」と
      書いたのは名前で探していたため）。
      **PR: asaomaro/ts5250#383（draft）**（work: `ts5250/.aidev/works/20260829-offline-regression-ci`）。
      - あちらには **CI が無かった**（`.github/workflows/` が存在しない）。約 5,400 の
        テストと `lint` / `build` が揃っているのに PR で回っていない。
      - `lint` → `build` → `test` の 1 ジョブを足した。手元では 3 コマンドとも rc=0。
      - **CI は初回で落ち、それが収穫だった**。手元（Node 24）では緑なのに
        **runner（Node 20）で 2 件落ちる**。どちらもテストが環境を前提にしている箇所:
        - `ebcdic` の CCSID 1252 でユーロ記号が往復しない。`reverseTableFor` が
          逆引き表を**ランタイムの `TextDecoder`** から作るため ICU 差で表が変わる。
          **`engines.node` は `>=20` なのに下限で動いていない**。
        - `server` の `printer-output-runtime` で、runner に**等幅 CJK フォントが無く**、
          PDF 保存に到達する前にフォントの警告が出る。
      - **緑にする手はどれも筋が悪い**（Node を 24 に上げる＝非互換を隠す／他人のテストの
        期待値を外から書き換える／その 2 ファイルを CI から外す＝回帰の目的に反する）。
        **draft のままにして判断を残した。**
      **決定（2026-08-29）: 2 件を直してから CI を入れる。**
      - Node を 24 に上げる案は**採らない**——`engines.node: >=20` との食い違いを隠す。
        ebcdic の方は**本物の欠陥**で、`reverseTableFor` が逆引き表をランタイムの
        `TextDecoder` から作るため ICU 差で表が変わる。**逆引き表を静的に持てば直る**。
      - 2 ファイルを CI から外す案も**採らない**——回帰を入れる目的に反する。
      - フォント側は runner に等幅 CJK フォントを入れるか、フォント依存をテスト側で明示する。

- [ ] **E2E ハーネスを `packages/` の公開パッケージにする** (repo:**ts5250**) —
      決定（2026-08-29）: **`packages/` に公開パッケージとして設計する**
      （`tools/` の内部ツールには留めない）。
      `scripts/example-automation.mjs` は **79 行のテンプレート**（極小ハーネス ＋ 薄い
      `Session5250` ドライバ）で、実行に**実機（pub400）の接続が要る**。
      設計で決めること: **公開 API・レポーターの形・フィクスチャ管理**。
      公開する以上、互換性の約束とドキュメントの負担を負う前提で進める。
      出所: `20260829-mcp-wiring-stocktake`（改名の判明にともなう棚卸し）。
- [ ] PowerVS トライアル環境の手順を文書化する — 申請〜接続〜片付け（設計書 5.1）
- [ ] チーム向け導入手順を書く — 一人検証の完了後（設計書 5.3）
