# レビュー: F4 の統合テストが止まる問題

## ラウンド 1（自己レビュー）

### 要件適合

- AC1 走って緑 → 手元で **3 passing / 終了コード 0**。
- AC2 止まらない → `settlesOrKeepsRunning`。1.7 秒で通る。
- AC3 対象は `test/integration` だけ → `testsRoot` を絞った。
- AC4 戻すと落ちる → **確かめた**。`await` の形は 20 秒で落ち、
  **他の 1 本は通った**（直す前はスイートごと死んでいた）。
- AC5 CI → `integration` ジョブ（`xvfb-run`）。**CI に判定させる**（D4）。

### 価値適合

「器は拡張ホストの中でしか確かめられない」——**1 本も回せなかったのが回るようになった**。
ただし**いま確かめているのは「例外なく起動できる」までで**、`WorkspaceEdit` や
undo はまだ書いていない。器が動くようになったので、次から足せる。

### 正確性

- **止まる形に戻して落ちることを確かめた**（AC4）。時間切れが無いと同じことが再発する。
- 待たない書き方は「reject しないこと」を見る——**通ることを保証しすぎない**。
  送信／取消の振る舞いは WebView の e2e が実物で見ている。

### 規約適合

- **走らせて確かめてから**「直った」と書いた（走らないものを直したと言わない）。
- CI は想像で決めず、載せて判定させる（D4）。

### 保守性

`teardown` で開いたエディタを閉じる。時間切れで再発を防ぐ。

### 指摘

- must: 0 / should: 0 / nit: 0

作業中に分かったこと:
1. 起票より**壊れている範囲が広かった**（D1）。`await` を直しても走らなかった
