# レビュー: テストの独立性を機械的に検品する

## ラウンド 1（2026-08-31）

差分: `tools/run-rpgunit.mjs`（`--order` / `--rclrsc` / `--check-independence` / `compareRuns`）/
`tools/README.md` / skill。

### 要件適合・価値適合

**問題なし。** AC 6 件すべて実機で検証済み。

価値（順序依存を機械的に弾ける）は **AC3 の発火確認**で裏づけられている。
設計書 4.2 が「人手の判断が要らない」と書いた検品が、実際に 1 コマンドになった。

### 指摘

#### must / should — なし

#### nit

- **N1**: `--check-independence` は実行を 2 回走らせるので所要が倍になる（実測 22.9s → 約 2 倍）。
  CI で毎回回すか、節目だけにするかは運用の判断。README には書いていない。
- **N2**: 対称な依存は捕まえられない（`decisions.md` D3 で明示し、代替手段を skill に書いた）。

### 規約適合

- **検査自体の検査**: わざと順序依存のテストを書いて発火を確認（AGENTS.md の流儀）。
- **実機にゴミを残さない**: IFS は道具が削除。スプールは消していない。
- **純粋な部分を分ける**: `compareRuns` は実機非依存で `--self-test` に 3 件。

### 判定

**must 0 / should 0 / nit 2。review 通過。**
