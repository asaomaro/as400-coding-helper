# 計画

1. `--order` / `--rclrsc` / `--check-independence` の解析
2. 実行部を関数化して 2 回呼べるようにする
3. `compareRuns`（合否だけ比較）
4. `--self-test` に 8 件追加
5. **実機で発火を確認**——独立したテストで一致、順序依存のテストで検出
6. 文書

## テスト方針

**検査が発火することを確かめるのが本体。** わざと順序依存のテストを書き、
終了コード 1 で捕まることまで見る。一致するケースだけでは検査が動いている証明にならない。
