# レビュー: 条件標識の解決と標識パネル

## ラウンド 1（自己レビュー）

対象: `git diff main` の実装差分（`.aidev/` を除く 11 ファイル / +868 -30）。
観点: 要件適合・価値適合・正確性・規約適合（AGENTS.md）・保守性。

### must

なし。

### should

**S1: 指摘の文言に無関係な標識が載っていた** — 直した。

`overlapsUnderIndicators` は設定中の標識を**すべて**文言に並べていたため、
`標識 01=オン, 02=オン, 50=オフ のとき FLD1 と FLD2 が重なります` のように
**その重なりに関係しない `50`** が載っていた。何を戻せば消える指摘なのかが読めなくなる。
→ `describeStatesFor` を足し、**その 2 つの項目が使っている標識だけ**を出すようにした
（`dspfRenderModel.ts`）。単体テストを 1 件追加（`50=` が載らないこと）。

**S2: 「すべて未設定」が効かない状態でも押せた** — 直した。

`Object.keys(this.indicators).length === 0` で判定していたため、別のファイルを開いたあとに残る
「そのファイルに存在しない標識の状態」でボタンが押せていた。その状態は**描画に効かない**
（項目の条件に現れる標識は必ず一覧にも出る）ので、押せると「何かが設定されている」と誤読される。
→ **一覧に出ている標識だけ**で判定するようにした（`ui.ts`）。

### nit

- **N1**: `renderIndicators` の JSDoc が字下げされていなかった → 直した。
- **N2**: `radioStep` が使わない `count` を受けており、`End` に到達しない分岐があった → 引数を落とした。
- **N3**: `describeConditioning` を `toRenderItem` で 2 回呼んでいた → 1 回にした。
- **N4**: `overlaps` を `occupanciesOverlap` に改名した際、旧名の別名定数を残していた → 呼び出し側を直して消した。

## 観点ごとの確認

### 要件適合

`spec.md` の「受け入れ基準との対応」表の 13 項目すべてに実装が対応している。
検証は `test.md`（AC ごとの確認表）。**AC-I1 だけ実装が文言と厳密には違う**——
見出しは残して中身を 1 行の説明にした（`decisions.md` D6 に理由を記録）。

### 価値適合

requirement のゴールは「**標識の組み合わせを指定すると、その組み合わせでの実機の見え方が
キャンバスに出ている状態**」。`indicators.dspf` を単独起動で開き、`50` を倒すと
`部門名` と `未定` が入れ替わることを e2e で実操作して確かめた。
`01` `02` を両方オンにしたときだけ重なりの指摘が出ることも確かめた
（**この組み合わせは既存の検出では原理的に報告されない**ので、これが新しく取れる情報にあたる）。

### 正確性

- 畳み込み（行 → 条件）は原典の 4 つの規則（1 行目は必ず新条件・`O` で新条件・`A`/ブランクで AND 継続・
  1 行目の `O` はブランク扱い）をそれぞれテストで固定した。
- 3 値は AND / OR それぞれで「偽が勝つ / 真が勝つ / 未知が残る」の 3 通りを固定した。
- **既定の見え方が変わらないこと**は値ではなく**参照**で固定した（`assert.strictEqual`）。
- 幅が読めない項目を重なりの判定から外す規則は、既存の `detectOverlaps` と揃えた。

### 規約適合（AGENTS.md）

- **原典照合は主エージェントが実施**した。`docs/origin/dds/FIELD-DSPF-pos716.html` の生テキストを
  抽出して読み、AND / OR の構造・上限（9 条件 × 9 標識）・`N` の位置・
  「最後の標識と同じ行に項目を書く」を確認している（`research.md` F1 に引用）。
- `languageId` / `contributes` / `activationEvents` は**無変更**（波及なし）。
- `sh` / `ps1` の CLI は**無変更**。

### 保守性

- 重なりの判定規則を写さず、`occupanciesOverlap` として `dspfLayout` から export して共有した。
- 桁の切り出しと妥当性の規則を写さず、`collectIndicators` は `readConditioning` を通す。
- `isMutuallyExclusive` / `readConditioning` の既存の戻り値は**変えていない**
  （ファイル診断の挙動に波及させないため）。

## 判定

must 0 / should 2（対応済み）/ nit 4（対応済み）。差し戻しなし。
差分が大きく（+868）複数モジュールを横断するため、**walkthrough を挟む**。
