# 計画: MCP 配線 3 件の棚卸し

## 作業順序と依存関係
1. **T1** ts5250 で 3 件の実体を探す（依存: なし）
2. **T2** MCP への登録経路を辿る（依存: T1）
3. **T3** 受け入れ条件と照らす（依存: T2）
4. **T4** backlog を更新（依存: T3）

## リスク / 留意点

| リスク | 対応 |
|---|---|
| **名前があるだけで済んだことにする** | 登録経路（`mcp-server.ts` → `registerHostServerTools`）まで辿る |
| 受け入れ条件を読み飛ばす | 項目の本文を逐語で読み、条件ごとに照合する |
| **ts5250 の未コミット作業を巻き込む** | あちらのコードは一切変更しない。読み取りのみ |

## テスト方針
- コード変更が無いので自動テストは増やさない。
- 根拠は**ファイル名と行番号**で示し、後から辿れるようにする。
