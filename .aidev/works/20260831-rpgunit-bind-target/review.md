# レビュー: テスト対象のバインド

## ラウンド 1（2026-08-31）

差分: `tools/run-rpgunit.mjs`（`--bnd` ＋ `SBMJOB` の成否確認）/ `tools/example/`（新規 4 本）/
`tools/README.md` / skill。

### 要件適合・価値適合

**問題なし。** AC 6 件すべて実機で検証済み。

価値（業務ロジックをテストできる）は **AC3 の壊す検査**で裏づけられている。
「RPGUnit が動く」から「**自分のコードをテストできる**」に進んだ。

### 指摘

#### must — なし

#### should — なし（工程中に 2 件見つけて直した。`test.md` 参照）

- `BNDSRVPGM` の書式（空白 → スラッシュ）
- `SBMJOB` の成否を見ていなかった

いずれも**実機に通す前に見つからなかった**種類で、`--self-test` では捕まらない。
前者は原典（`RPGUNIT/QCMD,RUCRTRPG`）を読んで確定した。

#### nit

- **N1**: `--bnd` は `MAX(50)` を超えても道具側で弾かない。実用上 50 個も束ねないので据え置く。
- **N2**: 実例の `addTax` は丸めの境界（`0.055` → 105）を 1 件だけ見ている。
  実例としては十分だが、**実務のテストならもっと境界を並べる**。README に含意がある。

### 規約適合

- **AGENTS.md「落ちることを確かめる」**: AC3 がまさにそれ。テストを触らず対象だけ壊した。
- **実機にゴミを残さない**: 壊した対象を戻して緑を確認済み。IFS も削除。
- **スプールを消さない**: 消していない。
- **原典で確定する**: `BNDSRVPGM` の書式を推測せず `QCMD` の定義を読んだ。

### 判定

**must 0 / should 0 / nit 2。review 通過。**
