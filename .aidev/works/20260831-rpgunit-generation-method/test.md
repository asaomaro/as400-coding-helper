# 検証結果: AI にテストを書かせる手順

## 受け入れ基準

| AC | 結果 | 根拠 |
|---|---|---|
| **AC1** 4 つの条件が手順に位置づく | ✅ | 0.1 が (1)(2)、0.3 が (3)(4) |
| **AC2** 「書く前 → 書く → 検品」の順序 | ✅ | 0.1 / 0.2 / 0.3 の構成 |
| **AC3** 出所を残す書式が機械で検査できる | ✅ | `VERIFICATION` / `CHARACTERIZATION` の印。どちらも無ければ差し戻せる |
| **AC4** 検品が実際のコマンド | ✅ | `run-rpgunit.mjs … --check-independence` と、ミューテーションの 4 手順 |
| **AC5** 設計書から辿れる | ✅ | 4.2 の冒頭に「実行手順は skill `rpgunit-test` の 0 節」 |
| **AC6** 例は実機に通したものだけ | ✅ | 下記 |

## 書式の実機確認

**載せた書式が動かないと手順の意味が無い**ので、ヘッダー付きのテストを実機に通した。

```
      * ==================================================================
      *  検証テスト（VERIFICATION）
      *  オラクル: 設計書 3.2「消費税は小数第 2 位まで保持する」
      * ==================================================================
     H NOMAIN OPTION(*SRCSTMT:*NODEBUGIO)
```

→ `SUCCESS 1 tests, 0 failure, 0 error`・終了 0。
**H 仕様より前の注記行として書ける**ことが確かめられた。

実例（`tools/example/CALCTST.rpgle`）にも同じヘッダーを付け、回帰なし
（`SUCCESS 2 tests, 0 failure`・独立性も一致）。

## 手順に載せた例の出所

| 例 | 出所 |
|---|---|
| `iEqual(105 : addTax(100 : 0.055))` が通ってしまう | **本セッションで実際に踏んだ**（PR #165） |
| 対象を壊して落とすミューテーション | **実機で実施**（PR #164） |
| `--check-independence` の検出 | **実機で発火確認**（PR #166） |

**すべて実機の実績で、作り話は無い。**

## 判定

**全 6 件の受け入れ基準を満たしている。差し戻しは無し。**
