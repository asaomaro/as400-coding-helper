# レビュー: AI にテストを書かせる手順

## ラウンド 1（2026-08-31）

差分: skill `rpgunit-test` に 0 節（+107 行）/ 設計書 4.2 の導線 / 実例のヘッダー。

### 要件適合・価値適合

**問題なし。** AC 6 件すべて充足。

価値（毎回思い出さなくても済む）については、**手順が skill の冒頭にあり、
テストを書く場面で必ず目に入る**位置にある点で満たしている。

### 規約適合

- **同じ内容を 2 か所に置かない**: 手順は skill 1 か所。設計書は方針のまま残し、
  冒頭にリンクを置いた（AGENTS.md「単一の真実源」）。
- **到達可能**: 設計書 → skill の導線を張った（「追加したリソースは到達可能に
  なって初めて完了」）。skill 自体は `description` のトリガーで引ける。
- **例は実機で通したものだけ**: 書式を実際にコンパイル・実行して確認。

### 指摘

#### must / should — なし

#### nit

- **N1**: ヘッダーの印（`VERIFICATION` / `CHARACTERIZATION`）を**機械で検査する仕組みは
  まだ無い**。「検査できる形」にはしたが、検査そのものは書いていない。
  lint 規則にするか、`run-rpgunit.mjs` が警告するかは次の判断。
- **N2**: 0.3 のミューテーションは手で回す前提。変異リストを持って自動で回す道具は無い。
  設計書も「変異を入れる」までは人／AI の判断としている。

### 判定

**must 0 / should 0 / nit 2。review 通過。**
