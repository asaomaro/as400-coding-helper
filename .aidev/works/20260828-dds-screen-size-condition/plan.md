# 計画: 画面サイズ条件名を編集する

## 作業順序と依存関係
1. 桁の書き戻しと短い形の分岐（依存: なし）
2. `screenSize` の検証と適用（依存: 1）
3. 受け渡し（protocol）と入力欄（依存: 2）
4. テストと e2e（依存: 3）

## リスク / 留意点
- **7 桁目はブランク**（標識のときの A/O とは違う）。読む側と対にする。
- 標識と混ぜられないことを検証で弾く（型では防がない＝house style）。
- `*DS4` を 24x80 の DSPF に書くと項目がキャンバスから消える。**正しい振る舞い**。

## テスト方針
- 往復（書いて読み直すと同じ名前）。
- 標識・空への切り替えで**元のソースと一致**すること。
- 排他と形の検査が断ること。
- 配線（protocol）をテストで固定する。
- e2e で実際に打って桁が変わることを見る。
