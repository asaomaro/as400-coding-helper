# テスト結果: キーワード欄の継続行を実機どおりに読む

## 実施

| 層 | 結果 |
|---|---|
| 単体（`npm test`） | **612 passing / 0 failing**（この work で +19） |
| 型検査（拡張ホスト / WebView） | 通過 |
| 原典検証（`npm run verify`） | 14 項目すべて ✓ |
| 桁位置 lint（DDS / PRTF / PF / LF / CMD / CL） | 指摘なし |
| GUI e2e（`dev/e2e.mjs`） | 66/66 PASS |
| **実機との突き合わせ**（`verify/verify-continuation.mjs`） | **✓ 定数 13 件が実機の解決結果と一致** |

## 直す前に落ちることの確認（AGENTS.md）

`src/core/dds/{ddsLogicalUnits,ddsEdit}.ts` を修正前に戻して同じ突き合わせを走らせると、
**4 件が「項目として読めていない」で落ちる**:

```
✗ 不一致 4 件
  - 3,12: 本 PJ が項目として読めていない（実機: "ABC   DEF"）
  - 5,12: 本 PJ が項目として読めていない（実機: "ABCDEF"）
  - 7,12: 本 PJ が項目として読めていない（実機: "ABC DEF"）
  - 9,12: 本 PJ が項目として読めていない（実機: "AAABBBCCC"）
```

**「消えていた」ことがそのまま出る。** 値が違うのではなく、項目が存在しなかった。

## 受け入れ基準ごとの確認

| AC | 確認 |
|---|---|
| AC1 | 単体（継続を含む定数 4 件がキャンバスに出る）＋ 実機突き合わせ |
| AC2 | 単体 `'ABC-` ＋ `   DEF'` → `'ABC   DEF'`（実機の Expanded Source と同値） |
| AC3 | 単体 `'ABC+` ＋ `   DEF'` → `'ABCDEF'`（同上） |
| AC4 | 単体 `'ABC` ＋ (50 桁)`DEF'` → `'ABC DEF'`（同上・長さ 7） |
| AC5 | 単体 `'AAA-` / `BBB-` / `CCC'` → `'AAABBBCCC'`（同上・長さ 9） |
| AC6 | 単体 3 件（継続は拒否 / 位置は通る / 別行のキーワードだけの項目は拒否しない） |
| AC7 | 既存 593 件が全通過。lint・`verify`・e2e も変化なし |
| AC8 | 実機の `Expanded Source` の定数 13 件と本 PJ の読み取りが一致 |

## 実機の環境（再現の前提）

- `SR-OSAKA` / IBM i **7.3**（`5770SS1 V7R3M0`）/ ライブラリ `ASAOLIB`
- コンパイル先は `QTEMP`（**オブジェクトを残していない**）。ソースメンバーは `ASAOLIB/QDDSSRC`
- 5250 の装置は既存の `ASA1`-`ASA4`（**自動構成は許可されていない**——新しい装置名は `8940`）

## 未検証の穴

- **PRTF / PF・LF の継続は実機で確かめていない。** 同じ `toLogicalUnits` を通るので
  同じに直っているはずだが、**確認したのは DSPF だけ**。
- **`keywordAreaOf` は 80 桁で切っていない**（`decisions.md` D3）。
  いまのサンプル・テストに 80 桁超の行が無いので差は出ないが、
  80 桁を超える行を読ませると本 PJ と実機で食い違う。backlog へ。
- **実機の突き合わせは CI で走らない**（実機と ts5250 の道具が要る）。手元でのみ。
- **DBCS を含む継続は実機の画面で見たが、突き合わせスクリプトには入れていない**
  （ソース物理ファイルの CCSID を分ける必要があり、`verify/` を複雑にするため）。
  結果は `research.md` F3/F4 に記録した。
