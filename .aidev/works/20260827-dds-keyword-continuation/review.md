# レビュー: キーワード欄の継続行を実機どおりに読む

## ラウンド 1（自己レビュー）

対象: `git diff main`（core 2 本 ＋ 新規テスト 1 本 ＋ PJ skill / 設計文書 / backlog）。

### must

なし。

### should

**S1: 機能欄が空の行まで継続に吸い込んでいた** — 直した。

条件付けだけの行（`01` を 9 桁目に書いた行など）は**次の単位への前置き**で、
キーワード欄は空。継続の連鎖がそこへ届くと吸い込んでしまい、
**その条件が次の項目に付かなくなる**。`tail.length === 0` で切るようにし、テストを足した。
実機の突き合わせは変わらず一致。

### nit

- **N1**: 実機突き合わせスクリプトの正規表現が、**リテラルの後ろにキーワードが続く行**
  （`'XYZ' COLOR(RED)`）を読み落としていた → 直した（13 件すべて読めるようになった）。

## 観点ごとの確認

### 要件適合

AC1-AC8 すべて `test.md` に確認あり。**AC8（実機との一致）は自動化してある**
（`verify/verify-continuation.mjs`）。

### 価値適合

ゴールは「**継続行を含む DSPF でも、実機と同じ値・同じ桁で読めている状態**」。
実機で通る DSPF の定数 13 件が、コンパイラの解決結果と 1 件残らず一致した。
**直す前は 4 件が「項目として存在しない」状態**で、値が違うのではなく消えていた。

### 正確性

- 継続の 3 通り（`-` / `+` / 記号なし）と優先順位（`-` → `+` → 引用符）は
  **実機の実測**で固めた。`''` のエスケープも含む。
- 連鎖・リテラルの外での分割・注記行での打ち切り・次行が無い場合・機能欄が空の行を、
  それぞれ単体で固定した。
- **既存の「キーワードだけの行は空白 1 つで連結」は変えていない**
  （`OVERLAY` を別行に書く形。既存 593 件が回帰の網）。

### 規約適合（AGENTS.md）

- **原典が無いので実機に判定させた**——AGENTS.md が明示的に認める経路。
  副作用の無い形（`QTEMP` へのコンパイル）で行い、**オブジェクトを残していない**。
- 実機を触る手順は PJ skill（`ibmi-remote`）に反映した。
  **「DBCS を含む場合は未確認」を解消**し、コンパイル・リストの取り出しを足した。
- **「テストを足したら、直す前の状態に戻して落ちることを確かめる」**——確かめた
  （`test.md` に落ちた出力を貼ってある）。

### 保守性

- 継続の規則は `joinContinuations` の 1 か所。`toLogicalUnits` は結果を使うだけ。
- 引用符の状態の判定は `ddsKeywords.ts` の `skipQuoted` と**同じ規則**だが、
  実装は別（あちらは区切りを返し、こちらは真偽を返す）。
  共通化すると片方の都合がもう片方に漏れるので、**規則をコメントで結び付ける**に留めた。

## 判定

must 0 / should 1（対応済み）/ nit 1（対応済み）。差し戻しなし。
core の中心を触り、実機の証拠が判断の要になるので **walkthrough を挟む**。
