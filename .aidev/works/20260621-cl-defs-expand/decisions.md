# 意思決定ログ（20260621-cl-defs-expand / autonomous）

## D1: 原典ソースをローカル HTML に差し替え（spec）

- **決定**: 定義の正のソースを、`cl-command-def` skill 既定の「IBM Documentation ライブ取得(Playwright)」では
  なく、PR #42 が収集済みの **`docs/origin/cl/<CMD>.html`（IBM i 7.4 / `ssw_ibm_i_74`）** とする。
- **理由**: 同一版の原典が出典固定（`docs/origin/manifest.yml`）で保存済み。403/SPA 回避・再現性・オフライン
  実行可。issue #43 のスコープ（収集済み原典の消費）と一致。
- **不変**: マッピング規約・検証規約（主エージェントによる原典直読・機械 diff）は skill に従う。取得方法のみ差替。

## D2: 85件を1PRに詰めず優先バッチで区切る（spec/plan）

- **決定**: 本作業（1 PR）では (a) 全85件を `.aidev/backlog/cl.md` に分類追記し、(b) 優先カテゴリ **P1（制御構造）を
  全件**原典照合済みで実装する。余力に応じ P2 以降へ拡張。未着手カテゴリは backlog `[ ]` で残し後続 batch で消化。
- **理由**: 85件は規模差が大きく、原典照合は主エージェント直読が必須（委譲不可）で1セッションで全件は非現実的。
  完了条件①（優先度順に作成）②（原典一致）③（backlog 追跡）は優先バッチ＋backlog 反映で満たせる。
- **再開性**: backlog の `[ ]`/`[x]` が単一の真実。後続は `aidev-util-batch` で未処理を順次消化できる。

## D3: F4 機能性の検証は「ロード可能性＝代理指標」とする（spec/test）

- **決定**: headless 環境で対話的 F4 を再現できないため、受け入れ①の機能性は「JSON がローダーでパース・
  `PrompterDefinition` 適合・キーワード照合でロードされる」ことを構造検証＋既存 unit テストで担保する。
  代表1件の手動 F4 スモークは推奨（記録に留め、必須ゲートにしない）。
- **理由**: ローダー（`jsonDefinitions.ts`）は不正 JSON を無言スキップするため、構造健全性が機能の必要十分に近い。
