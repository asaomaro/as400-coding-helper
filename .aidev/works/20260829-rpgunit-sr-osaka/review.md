# レビュー: RPGUnit を SR-OSAKA に導入する

## ラウンド 1（2026-08-29）

コード変更は無し（実機作業と工程文書のみ）。観点は**手順の妥当性と、実機に残した状態**。

### 要件適合・価値適合

**問題なし。** AC 6 件すべて `test.md` に根拠つきで対応。価値（固定長で実機テストが回る）も、
`**free` を 1 行も含まないソースで実際にコンパイル・実行できたことで裏づけられている。

### 指摘

#### must — なし

#### should

- **S1（対応済み）**: 判定手段の誤り。SAVF のバイト列から `TGTRLS` を読もうとして
  ユーザーに誤った選択肢を提示した。`decisions.md` D2 に記録し、
  **判定は `DSPSAVF`** と `spec.md`・`research.md` に明記した。
- **S2（対応済み）**: 非対話のジョブを `MSGW` で残した。`ENDJOB` で片付け、
  以後 `INQMSGRPY(*DFT)` を必須とした（`decisions.md` D4／`spec.md` エラー処理）。
- **S3（対応済み）**: `DLTLIB` 失敗を確認せずに `RSTLIB` を続け、v4/v6 が混在した。
  `CLRLIB` で入れ直して解消（`decisions.md` D3）。
  **教訓は spec の「エラー処理」に落とした**——前段が失敗したら次へ進まない。

#### nit

- **N1**: `ASAOLIB` に検証用の残置物が 5 件ある（`QUNITSRC`/`FIXTST2`/`RUDRV`/`RUDRV2`/
  `RPGUNIT` savf）。再現に要るので残したが、不要になったら消せる。`test.md` に一覧を書いた。
- **N2**: スプールを十数件出した（消していない）。`test.md` で報告済み。

### 規約適合

- **AGENTS.md「実機確認には対照を置く」**: 守った。実際に対照が誤読を止めた（`test.md`）。
- **memory「実機 probe はスプールを消さない」**: 守った。名前を報告している。
- **原典照合を主エージェントが行う**: 守った（配布物・`upload_savf.cmd`・原典ページを直読）。
- **共用機への負荷**: SR-OSAKA は自機。pub400 へのアクセスは今回ゼロ。

### 判定

**must 0 / should 3（いずれも工程中に対応済み）/ nit 2。review 通過。**
