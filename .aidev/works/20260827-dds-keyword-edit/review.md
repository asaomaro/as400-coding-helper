# レビュー: キーワード欄の編集

## ラウンド 1（自己レビュー）

対象: `git diff main`（core 2 本 ＋ プロトコル ＋ UI ＋ 新規テスト ＋ 実機の検証スクリプト）。

### must

**M1: 様式のキーワードが編集できなかった** — 直した。

`setKeywords` は `itemUnitAt`（`kind === "item"` だけ）で単位を引いていたため、
**様式（`R XXXX`）を選んで編集すると `line-not-found` で拒否された**。
`OVERLAY` / `CFnn` は様式にしか書けないので、**前 work で「様式も選べる」ようにした意味が消えていた**。

→ `unitAt`（種別で絞らない）を足し、**キーワードの編集だけ**がそれを使うようにした。
位置・長さ・削除は従来どおり項目だけを相手にする（様式に位置は無い）。
単体 2 件（様式の編集が通る / 様式への `move` は `line-not-found`）と e2e 1 件を足した。

**UI からしか踏めない不具合で、core の単体テストだけでは出なかった。**

### should

なし。

### nit

- **N1**: `keywordRunOf` が空配列で `NaN` を返しうる → `undefined` を返すようにした。
- **N2**: 実機の検証スクリプトの解析が、リストの折り返し行（行番号は付くが長さ欄が無い）を
  拾えていなかった → 直した。
- **N3**: e2e の `chipTexts` が `＋ 追加` を数えていた → キーワードだけを数えるようにした。
- **N4**: e2e で `constantLine` を再宣言していた（`SyntaxError`）→ 改名した。

## 観点ごとの確認

### 要件適合

AC1-AC8 と AC-I1〜I5 すべて `test.md` に確認あり。**AC8 は実機で確認**。

### 価値適合

ゴールは「**キーワードの追加・削除・引数の変更がデザイナの中で完結している状態**」。
単独起動で `MSGTXT` に 4 つ並べて溢れさせ、切れ目で折られること・読み直しで同じ並びに戻ること・
`✕` で外れること・`＋` で足せること・様式にも足せることを実操作で確かめた。

### 正確性

- **往復は総当たり**（原典の表 176 件 × 3 形）。手で並べた数件では実データの綴りを踏めない。
- **実機で解決値が一致**。特に二重の空白を含むリテラルの長さ 25 が保たれることは、
  `+` を使っていたら壊れていた（`decisions.md` D2）。
- 拒否は 2 つだけ（リテラルが消える / 注記行が挟まる）。どちらも**黙って壊れる**類。

### 規約適合（AGENTS.md）

- 折り返しの規則は**実機で確定したもの**を使い、書き出した結果も**実機で確かめた**。
- 「書けないものだけ拒否」——引数の中身も使用レベルも検証していない（`spec.md` に明記）。
- 検証スクリプトは再現できる形で work に残した。

### 保守性

- 折り返しは `foldKeywordArea` の 1 か所。`ddsEdit` は塊を行にするだけ。
- `setAttributes` の既存経路は**変えていない**（継続にまたがるときだけ新しい経路へ回る）。

## 判定

must 1（対応済み）/ should 0 / nit 4（対応済み）。差し戻しなし。
core・プロトコル・UI・実機の検証にまたがるので **walkthrough を挟む**。
