# レビュー: 複数ページの帳票

## ラウンド 1（自己レビュー）

### 要件適合

- AC1 後戻りで改ページ → 単体 4 件。**改ページを外すと落ちる**ことを確認済み。
- AC2 項目がページを持つ → 単体 2 件。
- AC3 UI / CLI で切り替えられる → e2e 6 件 ＋ CLI で実測。
- AC4 位置が原典の例と一致 → 単体 4 件（7 / 6.875 / 7.5 インチ）。
  **LPI を見ないと落ちる**ことを確認済み。
- AC5 LPI がファイル・レベルへ戻る → 単体 2 件。
- AC6 既存の絵が変わらない → 既存の帳票テスト（配置・描画・CLI・golden）が全通過。

### 価値適合

「1 枚に重ねて描かれると、どちらのページの項目か分からない」——
**逆走していた**のが直った（40 行目のあと `SKIPB(3)` で行が 3 に戻っていた）。

### 正確性

- **原典の計算例をそのままテストにした**。3 つの数（7 / 6.875 / 7.5 インチ）が
  一致するので、位置の積み上げ・スキップ先の求め方・行送りの 3 つが同時に裏づく。
- 処理の順序（LPI → SKIPB → SPACEB → SPACEA → SKIPA）を原典どおりにした
  ——`applyAfter` は SPACEA が先で SKIPA が後。
- 範囲の外のページは端に丸める（空の絵を出さない）。

### 規約適合

- **原典の生テキストに戻って仕様に落とした**。しかも**探す場所が違った**ことを
  `decisions.md` D2 に残した（キーワードごとのページだけを見ると落ちる規定がある）。
- 絞り方を CLI と UI で共有した（2 通りにしない）。

### 保守性

`Cursor` の注記に、なぜ位置と行番号の両方が要るのかを原典の引用つきで書いた。

### 指摘

- must: 0 / should: 0 / nit: 0

作業中に自分で見つけて直したもの:
1. 最初はモデル側で 1 ページに絞っていた（往復で絵が消える。D3）
2. CLI 確認用の帳票の桁を手で書いてずれた（D5。このバッチで 4 度目）
