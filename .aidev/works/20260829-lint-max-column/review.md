# レビュー: lint の桁上限を設定化する

## ラウンド 1（2026-08-29）

差分: 実装 6 ファイル ＋ `package.json` ＋ テスト 3 ファイル（+349 / -14）。

### 要件適合・価値適合

**問題なし。** requirement の AC 7 件すべてに spec の対応があり、test 工程で検証済み。
価値（レコード長 92 のソース PF で実機に入れる前に切り捨てを検知できる）についても、
設定の説明文がデータ桁 = レコード長 − 12 と代表値（112→100 / 92→80）を案内しており、
利用者が自分の値を決められる形になっている。

### 指摘

#### must

- **M1: `package.json` のインデントが崩れている**（`package.json:250-256`）
  追加した `rpgClSupport.lint.maxColumn` のプロパティが 6 スペース、閉じ括弧が 4 スペースで、
  周囲（キー 8 スペース / プロパティ 10 スペース）と食い違う。さらに**直後の
  `"rpgClSupport.lint.rules"` のインデントまで 8 → 4 スペースに巻き込まれている**。
  JSON としては妥当なので検査は通るが、差分が実際より大きく見え、次に触る人が混乱する。

#### should

- **S1: `USAGE` に Markdown の強調が混入している**（`src/cli/lint.ts:41`）
  `入れる値はレコード長ではなく**データ桁数**（レコード長 - 12）。` の `**` は
  **ターミナルにそのまま出る**。`USAGE` は `console.error` で端末に出す文字列で、
  既存の本文（`--c-new-opcode` の説明など）は装飾記号を使っていない。
- **S2: テストに意味のない assert が残っている**（`test/unit/lintCli.test.ts`
  「使い方に、設定と同じ値を渡すべきことが書いてある」）
  冒頭の `const { text } = lint([]); assert.ok(text !== undefined);` は、
  直後のコメント（「USAGE は --help のときだけ出るので、そちらで確かめる」）のとおり
  **何も検証していない**。lint を 1 回余計に走らせて捨てているだけなので削る。

#### nit

- **N1: テストが一時ディレクトリを片付けない**（`test/unit/lintCli.test.ts` の `fixture()`）
  `mkdtempSync` の後始末が無く、1 回の実行で 10 個ほど残る。
  既存テストにも同種のものがあるため本 work では直さないが、記録しておく。

### 判定

**must 1 件・should 2 件のため coding へ差し戻す。** いずれも振る舞いには影響しないが、
M1 は次に触る人に無用な差分を見せる。

## ラウンド 2（2026-08-29・差し戻し後の再点検）

### 前ラウンドの指摘の解消

| ID | 状態 | 確認 |
|---|---|---|
| **M1** `package.json` のインデント崩れ | ✅ 解消 | 差分が**純粋な追加 7 行だけ**になり、`rpgClSupport.lint.rules` を巻き込まなくなった |
| **S1** `USAGE` に Markdown の強調 | ✅ 解消 | `node out/cli/lint.js --help` の実地出力で `**` が消えていることを確認 |
| **S2** 意味のない assert | ✅ 解消 | 該当行を削除。テスト件数は 1167 のまま（死んだ assert であってテストではなかった裏付け） |

### 再点検

- **要件適合**: AC 7 件すべて test 工程で検証済み（`test.md`）。変更は文言とインデントのみで、
  受け入れ基準に触れていない。
- **正確性**: `npm test` 1167 passing / 0 failing / 0 pending。`verify:defs` 15 本・
  `verify:roundtrip` 538 定義とも OK。
- **規約適合**:
  - `src/lint/` は `vscode` を import していない（`verify-lint-core.mjs` が純粋性 42 ファイルを検査）。
  - 既定値を 1 か所（`DEFAULT_MAX_COLUMN`）にしか置いていない
    （AGENTS.md「同じ概念集合を複数箇所で列挙しない」／`RuleSpec.severity` と同じ流儀）。
  - 追加テストが load-bearing であることを、実装を戻して 3 件・8 件落ちることで確認済み
    （AGENTS.md「テストを足したら、直す前の状態に戻して落ちることを確かめる」）。
  - 期待値（データ桁 = レコード長 − 12、値域の上端 32754）は**実機カタログの生データで裏取り**
    している（AGENTS.md「落ちることの確認は、期待値の正しさを保証しない」への対処）。
- **保守性**: 追加した公開シンボルは 4 つ（`DEFAULT_MAX_COLUMN` / `MIN_MAX_COLUMN` /
  `MAX_MAX_COLUMN` / `resolveMaxColumn`）で、いずれも `rules/index.ts` 経由で引く形に統一。
  規則本体が既定と値域を持ち、engine と 2 つの消費者がそこから引く一方向の依存になっている。

### 残す指摘

- **N1（nit・本 work では直さない）**: `test/unit/lintCli.test.ts` の `fixture()` が
  一時ディレクトリを片付けない（1 実行で 10 個ほど残る）。既存テストにも同種があるため、
  横断で直すべき性質のもの。

### 判定

**must 0 / should 0 / nit 1。review 通過。**
