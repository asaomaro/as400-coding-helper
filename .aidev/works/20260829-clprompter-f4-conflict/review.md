# レビュー

## ラウンド 1（2026-08-29）

差分は調査文書 1 ファイル。コードは変更していない。

### 要件適合

`AC1`〜`AC4` すべて満たす。`when` は**両者のマニフェストの生の値**を載せた。

### 正確性

- **どちらが勝つかを断定していない。** VSCode は拡張どうしのキーバインドに優先度を
  規定していないので、「最後に登録されたものが実行される／登録順は制御できない」と書いた。
- **`.clle` と `.cmd` は「環境次第」と書いた。** clPrompter は `contributes.languages` を
  持たないので、その `when` が真になるかは他の拡張に依存する。断定できない。
- **本 PJ 自身が衝突を生んでいる**点を書いた（`.clp` → `cl` の言語登録）。
  これは AGENTS.md の「languageId 変更時の下流波及」そのもので、記録に値する。

### 価値適合

利用者が直せる形（`keybindings.json` の 3 パターン）まで書いた。
「衝突する」だけでは US1 の価値に届かない。

## 判定

| 重大度 | 件数 |
|---|---|
| must | 0 |
| should | 0 |
| nit | 0 |

**通過。**
