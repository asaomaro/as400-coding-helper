# レビュー: 継続行にまたがる参照

## ラウンド 1（自己レビュー）

### 要件適合

- AC1 継続の後ろ側 → 単体 1 件。**戻すと落ちる**ことを確認済み。
- AC2 `&` が継続の先 → 単体 1 件。同上。
- AC3 様式の参照 → 単体 1 件 ＋ e2e 1 件。同上。
- AC4 **単独のキーワード行が畳まれない**（前 work の回帰） → 単体 1 件。
- AC5 実機で通る → `verify/verify-continued-rename.mjs`（対照つき）。
- AC-I1 継続でない行の見た目は不変 → AC4 と同じテストが行数も見ている。

### 価値適合

「折り方によって追随したりしなかったりするのは説明できない」——
36 桁しかない欄でキーワードが 2 つ並べば折れるのは普通なので、
**折り方に依存しない**ことが値打ち。

### 正確性

- 前 work の回帰を**明示的に固定**した（行数と様式の行の両方を見る）。
- 折り直しで行数が変わりうるが、`applyDdsEdits` は降順で返すので当てる側は不変。

### 規約適合

- 表も UI も変えていない（`renameReferenceResults` の走査だけ）。
- **対照を置いた**ので誤読を止められた（`decisions.md` D2）。

### 保守性

分岐は 1 か所で、なぜ 2 通りあるのかを関数の注記に書いた。

### 指摘

- must: 0 / should: 0 / nit: 0

作業中に自分で見つけて直したもの:
1. 実機の probe で**対照ごと落ちた**（`SFLCSRRRN` の置き場所。D2）
2. e2e の判定が緩く、**直す前でも通る**書き方になっていた（`||` の fallback を外した）
