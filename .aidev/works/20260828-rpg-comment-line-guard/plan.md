# 計画: RPG の注記行のガード

## 作業順序と依存関係

1. `isCommentLine` を分類器に置き、`classifyWithState` と `absorb` で使う
2. `ruler.ts` の写しを外す（依存: 1）
3. 単体テスト（依存: 1）

## テスト方針

- 単体: 7 仕様書すべてで注記行が分類されないこと / 注記でない行は変わらないこと /
  **注記が索引を汚さないこと**。
- 2 つのガードを**それぞれ**外して落ちることを確かめる。
