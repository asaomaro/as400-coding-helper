# レビュー: RPG の注記行のガード

## ラウンド 1（自己レビュー）

### 要件適合

- AC1 注記行は分類されない → 単体 1 件（7 仕様書すべて）。**外すと落ちる**。
- AC2 判定は 1 か所 → `ruler.ts` から写しを外した。
- AC3 回帰 → 単体 2 件 ＋ 既存の `RpgSpecContext` テスト 10 件が通過。
- AC4 索引を汚さない → 単体 2 件（注記あり / 無しの対照）。**外すと落ちる**。

### 正確性

- **理由づけの誤りを 1 件、自分で見つけて直した**（`decisions.md` D2）。
  ガードを外してもテストが通ったので、テストが確かめていたものが
  主張と違うと分かった。実際に効く経路（`lastRecordName`）に書き直した。

### 規約適合

- **同じ規則を 2 か所に置かない**（D1）。写しが原因の欠陥だったので、
  足す側ではなく共有側に置いた。

### 指摘

- must: 0 / should: 0 / nit: 0
