# レビュー: `EDTCDE` を書けるキーボード・シフトを種別ごとに正す

## ラウンド 1（2026-08-28）

### 要件適合
- AC1 ✓ 表示装置で `6Y 2O EDTCDE(J)` の幅が **9** になる（直す前は幅不明）。
- AC2 ✓ 印刷装置では `not-numeric`（実機が通さない形）。
- AC3 ✓ `S` とブランクはどちらの種別でも従来どおり。
- AC4 ✓ 必須引数。
- AC5 ✓ `verify/probe-edtcde-shift.mjs`。

### 価値適合
実機で通る画面ファイルの項目が「幅不明」として描かれていた。幅が出ないと
重なりもはみ出しも判定できないので、**デザイナで確かめる意味が薄れる**。

### 正確性
実機の表をそのまま実装にした。表示装置ファイルの原典（36-37 桁目）とも一致する
——**原典どうしが食い違っていた**（印刷装置は「S かブランク」、表示装置は
「編集が効くならシフトは Y」）ので、種別で分けるのが正しい読み方だった。

### 規約適合
- AGENTS.md「原典と実機が食い違ったら、実機のパーサーに判定させる」。
- 種別は必須引数（渡し忘れで黙って印刷装置の規則が当たるのを防ぐ）。

### 保守性
`EDITABLE_SHIFTS` の 1 か所で表現し、注記に実機の表と原典の引用を残した。

### 指摘
must 0 / should 0 / nit 0。
