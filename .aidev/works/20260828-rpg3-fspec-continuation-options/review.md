# レビュー: F 仕様 継続行の選択欄・記入欄を足す

## ラウンド 1（自己レビュー・2026-08-29）

対象: `F-SPEC.json`（+48 −1）/ `rpg3NumericColumns.test.ts`（+約 110）/ `AGENTS.md`。

### 要件適合

AC1〜AC5 すべてに対応する証拠がある（`verify/results.md`）。
**AC5（取りこぼしの限界）は当初「記録する」だけの予定だったが、
2 巡目を流して実際に取りこぼしを見つけた**ので、記録ではなく**設計で無害化**した
（選択肢で縛らない）。要件より強い着地。

`対象外` に手を出していない: ILE の F 仕様・継続行の中身の検証・英語版のいずれも無変更。

### 価値適合

目的は「継続行がプロンプターで書けてルーラーで桁が見える状態」。

- 書ける: `buildRpgLineText` が 53/54-59/60-65 に正しく置くことを確認。
- F4 が開く: 継続行（7-16 桁が空・53 桁目が `K`）も `classifyRpgSpecKeyword` が
  `F-SPEC` に分類する——**消費経路を名指しで確かめた**（AGENTS.md「到達可能になって初めて完了」）。
- 桁が見える: ルーラーは SEU の書式行（`QEDTMSG`）由来で、定義とは別経路。
  **要件に「ルーラーにも桁が出ない」と書いたのは私の誤り**——ルーラーは元から
  実機の書式行を出しており、今回の変更とは無関係。要件の記述を訂正した。

### 正確性（見つけたもの）

| # | 重大度 | 内容 | 対応 |
|---|---|---|---|
| 1 | **must** | 1 巡目の結果だけで `dropdown` にしていたら、**実機が受ける 5 語を打てなくする**ところだった（`SAVDS` `IND` `NUM` `ID` `COMIT`）。`ADDPFM SRCTYPE` と同じ形。 | 2 巡目を流して取りこぼしを確定させ、`text` にした。テストで固定。 |
| 2 | should | `CONTINUATION` の `placeholder` が `S`。help には「`K` か空白しか入らない」とある。**入力例が誤り**。 | `K` に直し、テストで固定。 |
| 3 | should | 「QRG2023 が出ない ＝ 有効」は**不在証明**になりうる。 | メッセージ本文を読み、15 件中 14 件が**語を名指しする別のメッセージ**であることを確認（`probe-msgsummary.mjs`）。 |
| 4 | nit | 要件に「ルーラーにも桁が出ない」と書いたが誤り（上記）。 | 要件を訂正。 |

### 規約適合（AGENTS.md）

- **「原典と実機が食い違ったら、実機のパーサーに判定させる」** — RPG III は原典が無いので
  実機の判定が唯一の根拠。副作用の無い形（1 本のコンパイルと構文エラー）で行い、
  作ったオブジェクト・メンバー・IFS ファイル・スプールは片付けている。
- **「対照を必ず置く」** — 先頭と末尾に `INFDS` / `ZZZZZZ`。**2 巡とも 4/4**。
  末尾でも効いているので途中で診断が止まっていないことも示せる。
  **実際この対照が誤読を止めた**——スプール名の取り違えで全件「有効」に見えた回を弾いた。
- **「消去法で決めない」** — 「QRG2023 が出ないから有効」で止めず、本文で裏を取った（#3）。
- **「網羅したつもりの一覧は、機械で検査する」** — 桁の重なりを全欄で機械検査するテストを追加。
- **「候補に無いものは検出できない」** — RPG III の命令コードと同じ限界。help と
  research に明記し、**調べ方（`QRG2023` の有無）まで残した**ので次の人が増やせる。

### 保守性

- **コードは 1 行も変えていない。** 定義 JSON と、それを守るテストだけ。
- help に**調べ方**を書いたので、語を増やしたい人が手順を再現できる。

### 積み残し（別項目として backlog へ）

- **`restricted:false` の `dropdown` は自由入力できない。** `<select>` である以上
  列挙外を打てず、`attributes.restricted:false`（検証を緩める指定）が効かない。
  **CL 側の 86 欄が同じ状態**（`ADDPFM` の `SRCTYPE` に `RPGLE` を打てない）。
  今回は `text` を選ぶことで回避したが、**回避であって解決ではない**。

## 判定

**must 1 件 / should 2 件 / nit 1 件——すべてこのラウンド内で解消。** 差し戻しなし。
