# レビュー: F4 プロンプターを VSCode 非依存に作り替える

## ラウンド 1（自己レビュー・2026-08-29）

対象: `git diff` 32 ファイル / +3,339 −2,244。
観点は要件適合 / 価値適合 / 正確性 / 規約適合 / 保守性（protocol.md「2.」）。

### 要件適合

`spec.md` の「受け入れ基準との対応」を AC ごとに辿った。**AC1〜AC7 / AC-I1〜I5 すべてに
対応する検証がある**（`verify/results-2026-08-29.md` の対応表）。

`スコープ外`と書いたものに手を出していないことも確認した——定義 JSON は無変更、
`applyChanges` / `clCommandParser` / `model.ts` の**判断ロジック**は無変更
（`model.ts` は引数を足しただけで、既定の振る舞いは不変。単体テストで固定した）、
F4 の起動経路（キーバインド・`resourceExtname`）は無変更（`verify-contributes` 緑）。

### 価値適合

`requirement.md` の目的は「**VSCode の外で動き、実際に操作して自動で確かめられる状態**」。

- 外で動く: `dev/out/prompter.html` を `file://` で開いて 7 定義が描画・確定できる。
- 自動で確かめられる: 57 件の e2e が CI で走り、**後退を戻すと落ちることを 6 件で確認済み**。
- 写しが無い: `binding.ts`（1,401 行）ごと消え、判定は `model.ts` を直接呼ぶ。

「動くようにした」だけで終わっていないか——**`AGENTS.md` の該当節が嘘になる**ことに
気付いて書き換えた（`String(...)` で埋め込む、という記述）。ここを直さないと、
次に触る人が消えた仕組みに合わせて実装する。

### 正確性（見つけたもの）

| # | 重大度 | 内容 | 対応 |
|---|---|---|---|
| 1 | **must** | **選択肢に無い値が確定で消える**。`ADDPFM SRCTYPE(RPGLE)` のように、実機が `Rstd=NO` と言う 86 欄では列挙値以外を書ける。`select` に一致する選択肢が無いとブラウザは先頭を選ぶか無選択になり、**どちらでも元の値が失われる**。**旧実装から続く欠陥**（旧コードは先頭の `*NONE` を書き戻していた）。 | `formModel.ts` の `withCurrentValue` で、いまの値が選択肢に無ければ先頭に足す。単体テスト 4 件を追加し、戻すと落ちることを確認。 |
| 2 | **must** | **入れ子で確定した値を `session.values` へ入れる行が死んでいた**。直後の `refresh()` が DOM から値を集め直すため、毎回上書きされていた。e2e が落ちなかったことで発覚。 | 値の持ち主を `session.values` に一本化し、`rebuild()` で描き直す。戻すと落ちる。 |
| 3 | should | **見えない欄に赤字が残る**。確定できたのに F10 を開くと、止めた覚えの無いエラーが並ぶ。 | `refresh()` で、咎めない欄は表示上も咎めない。e2e で固定。 |
| 4 | nit | `startPrompter` が `PrompterUi`（描画モデルを覗く口）を返していたが、**誰も使っていない**。 | 削除。e2e は実際の DOM を見る。 |

### 規約適合（AGENTS.md）

- **「追加したリソースは到達可能になって初めて完了」** — 束ねた資産の到達性を辿った結果、
  **F5 の `preLaunchTask` が `compile` だけ**で、起動すると画面が真っ白になることが分かった
  （DDS ビジュアルエディタも同じ穴だった）。`compile:all` を足して `.vscode/tasks.json` を向けた。
- **「網羅したつもりの一覧は、機械で検査する」** — 「UI に `vscode` を持ち込まない」は
  型検査（`types: []`）に加え、`prompterWebview.test.ts` が**実ファイルを読んで**検査する。
  ファイルが増えたら一覧の突き合わせで落ちる。
- **「テストを足したら、直す前の状態に戻して落ちることを確かめる」** — e2e 6 件・単体 1 件で実施
  （`verify/e2e-load-bearing.md`）。**1 件は落ちず、そこから欠陥 #2 が見つかった。**
- **「改名・削除の直後は `rm -rf out out-test`」** — 実施（`binding.ts` / `help.ts` / `tiny-dom.ts` を削除）。
- **「同じ概念集合を複数箇所で列挙しない」** — `ObjectKind` の 3 値が `types.ts` と
  `workspaceObjects.ts` に二重にあったのを `types.ts` に寄せ、`ParameterDefinition.objectKind` の
  リテラル列挙もそれを指すようにした。

### 保守性

- 移した純粋関数（`commandText.ts` 439 行）は**中身を 1 行も変えていない**。
  往復検証 538 定義が緑であることで担保している。
- `ui.ts` は 783 行だが、判断を持たないので**読む単位は「描く」と「押されたら伝える」の 2 つ**。
  DDS の `ui.ts`（2,457 行）と同じ並び・同じ用語にしてあるので、片方を読めばもう片方も読める。

### 既知の制限（後退ではない）

- **`retainContextWhenHidden: false` のまま**。プロンプターを隠して再表示すると
  入力が初期値に戻る。旧実装（状態を HTML に焼き込み）でも同じ挙動で、**変わっていない**。
  直すなら別 work（`true` にする判断はメモリと引き換え）。

## 判定

**must 2 件 / should 1 件 / nit 1 件——すべてこのラウンド内で解消。** 差し戻しなし。
