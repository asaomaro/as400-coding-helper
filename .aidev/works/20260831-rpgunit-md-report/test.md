# 検証結果: Markdown レポート

## 実機で 3 通り出した

| 場面 | 結果 |
|---|---|
| **成功**（`CALCTST --bnd --check-independence`） | 判定 `SUCCESS`、独立性「一致（正順・逆順）」、環境 6 項目 |
| **テスト失敗**（`V2TST`） | 「失敗の詳細」に message ＋ Callstack が出た。終了 **1** |
| **独立性の食い違い**（`DEPTST`） | 「独立性の食い違い」節が出て `TESTUSES 合格/失敗`。終了 **1** |

**失敗しても独立性が食い違ってもレポートは出る。** 失敗したときこそ報告書が要るので、
食い違いの検出で即 `return` せず、`--md` / `--xml` を書いてから終了コードを返すようにした。

## 出力の実物（成功時・抜粋）

```
| 判定 | **SUCCESS** |
| 対象 | `ASAOLIB/CALCTST` |
| 独立性の検品 | 一致（正順・逆順） |

| | テスト | 判定 | assertions | 時間 |
| ✓ | `TESTROUND` | 合格 | 1 | 11.81 |

| os.version | `V7R3M0` |
| irpgunit.version | `v6.0.2.v` |
```

**環境は XML の `<properties>` 由来**なので、どの機械・どの版で回したかが
レポートだけで分かる。

## `--self-test`（40 件）

テンプレート・エンジンに 7 件、`reportData` に 4 件を追加。

| 見たもの |
|---|
| 値の埋め込み／未定義は空／注記は出力されない |
| 配列は繰り返す／空配列・偽は丸ごと省く／真なら 1 回（親の値が見える） |
| 判定・合格数・バインド無しの表記／合否の印／失敗だけを詳細に回す／失敗が無ければ節を出さない |

## 判定

**差し戻しは無し。**
