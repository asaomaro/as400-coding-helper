# レビュー: 画面サイズの「位置の上書き行」を配置に反映する

## ラウンド 1（2026-08-28）

### 要件適合
- AC1 ✓ 上書き行が直前の項目に付く（原典の例 2 / 例 3 の形をテストにした）。
- AC2 ✓ デザイナの切替と CLI の `--screen-size secondary`。
- AC3 ✓ 上書きが無い項目は 2 次でも同じ位置。
- AC4 ✓ 実機の 9 通り（形 5 ＋ キーワード 3 ＋ 比較 1）と一致。
- AC5 ✓ 2 次では動かせない（e2e でドラッグしてソースが変わらないことを見た）。

### 価値適合
**2 次画面サイズの見え方が一切見えなかった**状態を解消した。上書き行は論理単位に
すらならず捨てられていたので、書いても何も起きなかった。

### 正確性
- **モデルの取り違えを直した**（D1 の背景）。「その画面サイズでだけ現れる項目」ではなく
  「同じ項目の別の位置」。
- 見分けは位置の有無（D2）。OR 条件の扱いは変えていない。
- 2 次で掴めると**黙って 1 次の位置を壊す**（D3）。早期 return で塞いだ。
- 行の形の規則は実機が決めた（D4）。**原典と食い違う**ので、その旨を注記に残した。

### 規約適合
- AGENTS.md「原典と実機が食い違ったら、実機のパーサーに判定させる」。
- 「追加したリソースは到達可能になって初めて完了」——モデルに載せるだけでなく
  **デザイナの切替と CLI** まで通した。切替は 2 次が無ければ出さない。

### 保守性
解決を 2 回まわす形にしたので（D1）、`RenderItem` を読む側は 1 つも変わっていない。

### 指摘
must 0 / should 0 / nit 0。

**残っているもの**:
- 2 次画面サイズでの**編集**（D3）。位置を直すにはテキストエディタが要る。
- キーワードの条件付けは 3 つしか試していない（D4）。通るものが見つかれば緩める。
