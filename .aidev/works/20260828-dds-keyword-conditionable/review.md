# レビュー: 条件付けできないキーワードを知らせる

## ラウンド 1（2026-08-28）

### 要件適合
- AC1 ✓ 画面・帳票とも指摘する（キーワードの行を指す）。
- AC2 ✓ `generate-dds-conditioning.mjs` が原典 284 ページから生成。手書きの表は無い。
- AC3 ✓ `verify-dds-conditioning.mjs` が**実機の 5 件**と件数の下限を見る。
- AC4 ✓ `layout-keyword-not-conditionable`（既定 ON・error）。`package.json` にも露出。
- AC5 ✓ 記述が無ければ `undefined` で咎めない。
- AC6 ✓ 代表行は対象外。

### 価値適合
狙いは「実機に持っていく前に分かる」。直前の work で**条件を付ける手段を足した**ので、
デザイナから通らない形を作れるようになっていた——**足した機能の穴を同じ batch で塞いだ**形。

### 正確性
- **実機の錨が効いている**ことを、抽出を壊して確認した（食い違い 3 件＋件数不足 2 件で落ちる）。
- 偽陽性を出さない側に倒した（D2）。既定 ON・error なので偽陽性の代償が大きい。
- `nn` の総称は鍵を大文字に揃えて引く。片方だけ揃えると**黙って引けなくなる**
  （実際 `CAnn` が `undefined` になり、テストで捕まえた）。

### 規約適合
- AGENTS.md「原典から機械的に決まる成果物は、LLM に書かせずスクリプトで生成する」。
- lint 規則の到達性（`package.json` の `lint.rules` に載っているか）は
  既存の検査が見ており、載せ忘れで落ちた——**その検査が働いた**。
- 同梱サンプルに指摘が出ないことを確認済み（CI の lint は緑のまま）。

### 保守性
生成器の注記に「ひっかけの除外はいま効いていない」ことまで書いた（D4）。
効いているように読めると、後から原典が変わったときの判断を誤らせる。

### 指摘
must 0 / should 0 / nit 0。

**残っている穴**: 原典が可否を書いていない DSPF 17 件 / PRTF 1 件は判定できない。
実機で総当たりすれば埋まるが、キーワードごとに妥当なパラメータが要るので
別の work になる（いまは咎めないので害は無い）。
