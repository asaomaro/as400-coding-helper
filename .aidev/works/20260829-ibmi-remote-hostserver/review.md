# レビュー: `ibmi-remote` skill に hostserver 経路を足す

## ラウンド 1（自己レビュー・2026-08-29）

対象: `.claude/skills/ibmi-remote/SKILL.md`（+144 −0）。

### 要件適合 / 価値適合

AC1〜AC6 すべてに対応がある（`verify-notes.md`）。

目的は「**次に実機を触る人が、いま使っている経路と落とし穴を skill だけで把握できる**」。
落とし穴を**先に**（各節の冒頭に太字で）置き、手順を後に置いた。
一般論ではなく**実際に踏んだ例**を添えてある——読み飛ばされにくい形。

### 正確性

| # | 重大度 | 内容 | 対応 |
|---|---|---|---|
| 1 | should | frontmatter の `description` が「**ssh で行う**」のままだった。2 経路になったのに片方しか書いておらず、**hostserver の用途で skill が選ばれない**恐れがあった。 | 両経路と「実機で桁を確かめて」を足した。 |
| 2 | should | 「引用符の扱い」が 6 節の直前にあり、**hostserver にも掛かると読めた**。 | 「**ssh 経路のみ**」と明示し、hostserver には無いことを注記。 |
| 3 | nit | 6.4 と既存 4.5 が同じ性質のことを別々に書いている。 | 6.4 から 4.5 を参照。統合はしない（対象コマンドが違うため）。 |

### 規約適合

- **秘密を書かない**（`/workspaces/ts5250/.env` は読まない・値を出さない）。
  `grep` で確認済み。書いたのは `--env-file` の形と、`.env.verify` の識別子名だけ。
- **例を書き下ろさない**。すべて動いたコードから写した（出典を `verify-notes.md` に列挙）。
- **既存を消さない**（削除行 0）。ssh 経路は pub400 で今も唯一の手段。

### この work そのものの発端について

**起票は「レシピを skill 化する」だったが、着手前に現物を見たら大部分は済んでいた。**
AGENTS.md / `aidev-00-start` の「backlog は遅れる。着手前に現物で裏を取る」がそのまま効いた
——書いていなければ、既にある 206 行を**もう一度書いていた**。

一方で**逆の失敗も同じ日に踏んでいる**。`CRTRPGPGM` のスプール名を取り違えた件は、
**この skill の 4.5 節に既に書いてあった**（`CRTDSPF` の話としてだが同じ性質）。
「PJ 内部の仕様を先に読む」は backlog の裏取りだけでなく、**作業そのものにも効く**。
6.4 節にその経緯を残したのはそのため。

## 判定

**must 0 件 / should 2 件 / nit 1 件——解消済み。** 差し戻しなし。
