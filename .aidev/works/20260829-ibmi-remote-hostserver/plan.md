# 計画: `ibmi-remote` skill に hostserver 経路を足す

## 作業順序と依存関係

1. 冒頭に経路の分岐を足す（依存: なし）
2. 節 6 を足す（依存: 1）
3. 既存節と矛盾していないか全文を読み直す（依存: 2）

## リスク / 留意点

- **R1: 秘密の混入。** `.env` の値を書かない。`--env-file` の形だけ書く。
- **R2: 動かない例を書く。** 例は `verify/*.mjs` から写す。書き下ろさない。
- **R3: 長くなりすぎて読まれない。** 落とし穴を先に、手順を後に置く。

## テスト方針

skill は文書なので自動テストの対象ではない。確かめるのは 3 つ:

- 例が**実際に動いたもの**であること（出典を `verify/*.mjs` で示す）。
- 既存節と**矛盾しない**こと（全文を読み直す）。
- **秘密が入っていない**こと（`grep` で確認）。
