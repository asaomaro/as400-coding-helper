# 確認（2026-08-29）

skill は文書なので自動テストの対象ではない。plan の 3 点を確かめた。

## 1. 例は実際に動いたものか

| 節 | 出典 |
|---|---|
| 6.1 接続の型 | `.aidev/works/20260828-rpg3-fspec-continuation-options/verify/probe-options.mjs` |
| 6.2 最小のレシピ | 同上（`CRTSRCPF` → `CPYFRMSTMF` → `CHGPFM` → `CRTRPGPGM`。34 回動かした） |
| 6.3 ライブラリー・リスト | `20260828-rpg3-numeric-fields`（`CPF5715` を踏んで覆した経緯） |
| 6.4 リストの読み方 | `probe-options.mjs` の `listingCodes` / `probe-msgsummary.mjs` |
| 6.5 片付け | `.../verify/cleanup.mjs`（71 件が残っていた実測） |

**書き下ろした例は無い。** すべて動いたコードから写した。

## 2. 既存節と矛盾しないか

- **削除ゼロ**（`git diff` の削除行 0。144 行の追加のみ）。
- 4.5 節（`CRTDSPF` のリスト。「スプール名はメンバー名」）と
  6.4 節（`CRTRPGPGM` のリスト。「スプール名は対象の名前」）は**同じことを言っている**。
  6.4 から 4.5 を参照して繋げた。
- 「引用符の扱い」は ssh 固有なので、**hostserver 経路には無い**と 1 行足した
  （節が 6 の直前にあり、6 にも掛かると読めてしまうため）。

## 3. 秘密が入っていないか

`grep -niE 'password *= *"…|AS400_SECRET|172\.21\.'` → **該当なし**。
`--env-file` で渡す形だけを書き、`.env` の中身には触れていない。

## 受け入れ基準

| AC | 確認 |
|---|---|
| AC1 経路の判断が冒頭にある | 「どちらの経路を使うか」（機械ごとの表） |
| AC2 hostserver の最小レシピ | 6.0〜6.2 |
| AC3 `ADDLIBLE` が効かない | 6.3（`CPF5715` を誤診した経緯つき） |
| AC4 リストの読み方 | 6.4（スプール名 / 件数 / `GENLVL`） |
| AC5 対照と片付け | 6.5（対照が 30 語の誤りを止めた例 / 71 件残っていた例） |
| AC6 既存を壊していない | 削除行 0 |
