# レビュー

## ラウンド 1（2026-08-29）

差分は CI の workflow 1 ファイルのみ。生成器・定義・テストは触っていない。

### 要件適合 / 価値適合

目的「生成物を手で直したら CI が落ちる状態」を、**実際に手編集をコミットして**確かめた。
`prompter/dds` は以前は素通りしていた。

### 正確性

- **列挙をやめたのが要点**。3 つ並べる形だと、出力先が増えたとき足し忘れても
  何も起きない。実際 3 つ漏れていた（AGENTS.md「同じ概念集合を複数箇所で列挙しない」）。
- `resources/` に生成物以外があっても、**再生成で差分が出ない限り落ちない**ので害は無い。
  手元で全件回して差分ゼロを確認済み。
- **生成器の順序**は既存のコメントどおり維持した。崩すと `prompter/cl/en` が壊れる
  （手元で実測——`generate-cdml-rules` を CL 定義より先に回して 200 件超の差分を出した）。

### 保守性

`as400-web-emulator` 側を別項目に割った。1 つの項目に 2 リポジトリの作業が入っていると、
片方が済んでも閉じられない。

## 判定

| 重大度 | 件数 |
|---|---|
| must | 0 |
| should | 0 |
| nit | 0 |

**通過。**
