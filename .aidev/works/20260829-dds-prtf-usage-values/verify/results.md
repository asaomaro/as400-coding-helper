# 検証結果（2026-08-29）

| 検証 | 結果 |
|---|---|
| `npm test` | **1148 passing / 0 failing**（1145 から +3） |
| `npm run verify` | **rc=0**（往復検証 538 定義を含む） |
| プロンプター e2e | **71/71 PASS**（+2） |
| DDS エディタ e2e | **182/182 PASS** |
| `docs/src` 全 11 サンプル | **指摘 0 件** |
| 実サンプルの行で往復 | 38 行が 1 バイトも変わらず |
| 戻すと落ちるか | 3 通りとも落ちる（**5 / 2 / 2 件**） |

## 実機（IBM i 7.3・`CRTPRTF`）

| 形 | 受理 | 無効 |
|---|---|---|
| 位置あり 37 通り | **3 件（ブランク `O` `P`）** | 34 件 |
| 位置なし 37 通り | **3 件（ブランク `O` `P`）** | 34 件 |
| 対照（最小形） | 作成できる ○ | — |

原典（ブランク / `O` / `P`）と**完全一致** → `restricted: true`。

**位置あり／なしの両方を流した**のは、原典が `P`（プログラム - システム間）について
「位置は無効です」と書いているため。**どちらでも同じ集合**だったので、
位置の有無が 38 桁の判定に影響しないことが確かめられた。

## 残したスプール（消していない）

`U38CTL #1092` / `U38POS #1093` / `U38NOP #1094`（`075010/ASAO/QPRTJOB`）

## 受け入れ基準

| AC | 結果 |
|---|---|
| AC1 原典どおりの値・ja/en 一致 | ✓ ブランク / `O` / `P` |
| AC2 他の欄を変えない | ✓ 再生成の差分は `DDS-PRTF.json` のみ |
| AC3 一致したときだけ `restricted: true` | ✓ 対照つき 37×2 通り |
| AC4 `text` → `dropdown`・ブランクが選べる | ✓ 単体テスト＋e2e（実ブラウザで `<select>` と選択肢を確認） |
| AC5 サンプルで指摘 0 | ✓ 全 11 件 |
| AC6 テスト・検査・戻すと落ちる | ✓ |
| AC7 実機の残骸なし（スプールは残す） | ✓ オブジェクト・メンバー・IFS は削除。スプール 3 件は記録して保持 |
