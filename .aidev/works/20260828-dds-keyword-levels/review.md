# レビュー: キーワード使用レベル検査

## ラウンド 1（自己レビュー）

### 要件適合

- AC1 書けないレベルを指摘 → 単体 6 件（DSPF 3 形 ＋ PRTF 2 形）。
  **診断を外すと落ちる**ことを確認済み。
- AC2 実機がその形を通さない → `verify/probe-levels.mjs`（7 通り・**対照つき**）。
- AC3 知らないものを咎めない → 単体 2 件。**「判断しない」を外すと 5 件落ちる**。
- AC4 検証済みサンプルで偽陽性 0 件 → 単体 1 件 ＋ CLI で 9 ファイル確認。
- AC5 既定 ON ＋ 設定 → `package.json` に宣言。既存の一覧テストが一致を見る。

### 価値適合

「偽陽性が出る検査は切られ、切られた検査は無いのと同じ」——だから
**先に数えてから既定を決めた**（`decisions.md` D3）。

### 正確性

- **原典のレベルと実機が全件一致**した（7 通り）。
- ファイル・レベルは論理単位にならないので `fileLevelKeywordLines` から読む
  ——ここを忘れると `DSPSIZ` の誤りを見逃す。

### 規約適合

- **原典から生成**（手で並べない）。CI の再生成にも載せた。
- **既定 ON の基準を守った**（実測 0 件）。
- 束の重さを考えて軽い資源にした（D1）。

### 保守性

「一覧に無い ＝ 判断しない」を資源の `note` と型の注記の両方に書いた。

### 指摘

- must: 0 / should: 0 / nit: 0
