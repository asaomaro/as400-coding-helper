# 計画: 条件つきの `EDTCDE` を幅に効かせるか決める

## 作業順序と依存関係
1. 実機で確かめる（依存: なし）
2. 結果に応じて実装を直すか、理由を残す（依存: 1）
3. 網羅（条件付けできないキーワード一覧）は別項目として起票（依存: 1）

## リスク / 留意点
- 「たぶんこうだろう」で直すと**正しかったものを壊す**。先に測る。
- 前 work（`COLOR` / `DSPATR` を条件で解く）の前提も同じ probe で裏を取る。

## テスト方針
実機の probe が判定そのもの。実装を変えないので回帰のみ。
