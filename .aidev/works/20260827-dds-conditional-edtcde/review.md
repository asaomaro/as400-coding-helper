# レビュー: 条件つきの `EDTCDE` を幅に効かせるか決める

## ラウンド 1（2026-08-27）

### 要件適合
- AC1 ✓ 実機で確かめ、表にした（`decisions.md` D1 / `ddsFieldWidth.ts` の注記）。
- AC2 ✓ 「直さない理由」をコードに残した。
- AC3 ✓ `COLOR` / `DSPATR` が条件付けできることも同じ probe で確認。

### 価値適合
狙いは「いまの実装が正しいのかを根拠つきで知る」。**結果は「正しい」**で、
コードは 1 行も変えていない。ここで直していたら、
**存在しない状態を描く実装**にして正しかったものを壊していた。

さらに**前 work の前提の裏取り**になった（D2）。`COLOR` / `DSPATR` が条件付けできる
ことは、それまで実機で確かめていなかった。

### 正確性
判定は**副作用の無い形**（コンパイルするだけ。作ったファイルは消した）。
`B` と `O` の両方で確かめている（`EDTCDE` は出力可能フィールド向けなので両方要る）。

### 規約適合
- AGENTS.md「原典と実機が食い違ったら、実機のパーサーに判定させる」。
  今回は原典に規定が**無い**ケースだが、扱いは同じ。
- 網羅は手で書かず生成する項目として起票（D3）。

### 保守性
`keywordGroups` があるぶん「幅も条件で解けるのに解いていない」と読める。
**その誤読を防ぐことがこの work の成果物**。

### 指摘
must 0 / should 0 / nit 0。
