# レビュー: 描画全体を実機のゴールデンと突き合わせる

## ラウンド 1（2026-08-27）

### 要件適合
- AC1 ✓ 定数 14 件の全文字を桁で突き合わせ。DBCS・混在・端も含む。
- AC2 ✓ 入力欄 3 件を行・桁・表示桁数・保護で突き合わせ。出力専用が欄に出ないことも。
- AC3 ✓ 定数の先頭セルの色・反転表示・下線・明滅・非表示を突き合わせ。
- AC4 ✓ `npm test` で走る（実機に触らない）。750 件（+47）。
- AC5 ✓ `verify/capture-render-golden.mjs` と `verify/make-render-fixture.py` を work に残した。

### 価値適合
目的は「ずれたら落ちる」。**落ちることを 2 通りで確かめた**——
`Y` × 小数点の +1 を戻すと 2 件、`constantSegments` の DBCS を 1 桁に数えると 10 件。

さらに**この work 自体が欠陥を 1 件見つけた**（`Y` × 小数点あり × 入力可の +1 欠落）。
ゴールデンを入れる前は目視でしか見ておらず、数字の入力欄はどの画面にもあるのに
重なり・はみ出しの判定が 1 桁甘いまま通っていた。

### 正確性
- ゴールデンは**実機の事実だけ**（`cells` / `fields`）。モデルの語彙を含まない。
  期待値を写して固定する事故を構造で防いでいる（AGENTS.md）。
- 表示桁数の規則は**実機に判定させた**（原典に表が無い）。`decisions.md` D1/D2。
- `EDTCDE` があるときに二重に足さないことを D3 で明示。
- ゴールデンが読めなければ**落とす**（skip にしない）。skip は「合っている」と誤読される。

### 規約適合
- 原典に規定がある部分（表示桁数が増えること自体）は原典を引用し、
  規定が無い部分（増える量）だけ実機で決めた。区別が `dspfLayout.ts` の注記に書いてある。
- 検証は**主エージェントが実施**（委譲していない）。
- 様式は**生成して書き出す**（`make-render-fixture.py`）。桁を手で数えていない。

### 保守性
- ゴールデンの表現を 1 つに絞った（D4）。2 つ持っていたときに実際に取り違えた。
- 採取（実機・人が回す）と照合（`npm test`・CI）を切ったので、
  実機に繋げない環境でもテストは通る。

### 指摘
must 0 / should 0 / nit 0。

**未検証の穴**: `editedWidth` が DSPF の `Y` ＋ `EDTCDE` を弾く件（D5）。
いまは「分からない」と言うので誤描画にはならないが、描けない。backlog に残した。
