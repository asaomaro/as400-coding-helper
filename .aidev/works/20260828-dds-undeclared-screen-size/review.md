# レビュー: 使えない画面サイズ条件名を知らせる

## ラウンド 1（2026-08-28）

### 要件適合
- AC1 ✓ 使えない条件名を指摘する（理由を 3 通りに分けて）。
- AC2 ✓ **実機の 8 通りと全件一致**（テストにそのまま入れた）。
- AC3 ✓ `layout-invalid-screen-size-condition`（既定 ON・error）。
- AC4 ✓ 位置の上書き行でも指摘できる（生の行から探す）。
- AC5 ✓ 同梱サンプルは指摘ゼロ。

### 価値適合
デザイナでは項目が**黙って消える**だけで理由が出なかった。実機に持っていって
`CPF7311` を見るまで分からない状態を消した。

### 正確性
- **起票時の見立てが違っていた**（D1）。実機で 8 通り試して初めて
  「2 次を指していること」だと分かった。宣言の有無だけを見る実装は 2 通りで食い違う。
- **論理単位からは 1 行も見つからなかった**（D2）。上書き行は単位にならない。
  `collectIndicators` が同じ理由で生の行を見ており、先例に倣った。
- 描くこととコンパイルが通ることを分けた（D4）。

### 規約適合
- AGENTS.md「原典と実機が食い違ったら、実機のパーサーに判定させる」。
  今回は**原典に規定が無い**（条件名の使い方の詳細）ケースで、実機だけが答えを持っていた。
- 既定 ON・error にするので、同梱サンプルで指摘が出ないことを確かめた。

### 保守性
実機の 8 通りを表にして注記とテストの両方に置いた。判定を緩めると 4 件落ちる。

### 指摘
must 0 / should 0 / nit 0。

**残っているもの**: 位置の上書きを配置に反映していない（D3）。
2 次画面サイズの絵は描けないまま。backlog に起票した。
