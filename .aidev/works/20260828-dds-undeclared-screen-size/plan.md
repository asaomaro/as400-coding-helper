# 計画: 使えない画面サイズ条件名を知らせる

## 作業順序と依存関係
1. 実機で規則を確かめる（8 通り）（依存: なし）
2. 指摘を実装する（依存: 1）
3. lint 規則（依存: 2）
4. テスト（実機の表をそのまま）（依存: 3）

## リスク / 留意点
- **論理単位から探すと 1 行も見つからない**（上書き行は単位にならない）。
- 既定 ON・error なので、同梱サンプルに出ないことを確かめる。
- 既存のテスト（`*DS3` の項目を描く）は**描くこと**を見ており、
  指摘が増えるので更新が要る。描くこと自体は変えない。

## テスト方針
- 実機の 8 通りをそのままテストにする。
- 上書き行が論理単位に入っていないことを**前提として**固定する。
- 判定を緩めると落ちることを確かめる。
