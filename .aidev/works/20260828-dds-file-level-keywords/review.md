# レビュー: ファイル・レベルのキーワードをデザイナから読めるようにする

## ラウンド 1（2026-08-28）

### 要件適合
- AC1 ✓ `CUSTMNT.dspf` の 4 本が一覧に出る（e2e で並びまで固定）。
- AC2 ✓ チップに分かれ、原典の解説が引ける。
- AC3 ✓ `✕` / `＋` / 生テキストの編集を出さない（e2e で確認）。
- AC4 ✓ CLI の `parse` に `fileKeywords`。
- AC5 ✓ 最初の様式・項目で止まる。注記行・空行は飛ばす。
- AC6 ✓ 独自クラス。項目を選ぶ側は拾わない。

### 価値適合
`CUSTMNT.dspf` はキーワード行 8 本のうち **4 本がファイル・レベル**。
つまりこのファイルのキーワードの半分がデザイナから見えていなかった。

### 正確性
- **`toLogicalUnits` を変えずに済ませた**（D1）。読み手を 1 つも壊していない
  （「論理単位には入らない」ことをテストで固定）。
- 分類を 1 か所に切り出した（D2）。写していたら、同じ行の扱いが 2 通りになっていた。
- **クラス名の共有で実際に壊れた**（D4）。e2e が捕まえた——
  一覧の走査と様式の選択が、項目でも様式でもないものを拾った。

### 規約適合
AGENTS.md「追加したリソースは到達可能になって初めて完了」。
モデルに載せるだけでなく、**一覧から選べてプロパティに出る**ところまで通した。
CLI にも出したので、エージェントからも読める。

### 保守性
`fileLevelKeywordLines` は `joinContinuations` と `classifyDdsLine` を使い回すだけで、
新しい規則を 1 つも持っていない。

### 指摘
must 0 / should 0 / nit 0。

**残っているもの**: 編集できない（D3）。`setKeywords` の宛先を広げれば通せる。
`DSPSIZ` を読めるようになったので、**ユーザー定義の画面サイズ条件名が
宣言されているか**の検査もできるようになった（どちらも backlog に起票）。
