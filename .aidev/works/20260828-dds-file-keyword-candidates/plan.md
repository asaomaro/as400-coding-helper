# 計画: ファイル・レベルのキーワードの候補

## 作業順序と依存関係

1. `keywordsForLevel` / `KeywordLevel` を core に足す（依存: なし）
2. `ui.ts` の塞ぎを外し、絞りを core に委ねる（依存: 1）
3. 単体（絞りの規則）（依存: 1）
4. e2e（`＋` が出る・候補が絞られる・**足したものがソースに入る**）（依存: 2）

## リスク / 留意点

- **出すだけで届いていない**形になりやすい。AGENTS.md「追加したリソースは到達可能に
  なって初めて完了」——e2e で**ソースに入るところまで**見る。

## テスト方針

- 単体: 絞りの規則（file に DSPSIZ / field に出ない / 未設定はどこでも出る）。
- e2e: `＋` → 候補 → 選ぶ → ソースに入る、まで通す。
- 戻して落ちることを確かめる（塞ぎを戻す / 絞りを外す の 2 通り）。
