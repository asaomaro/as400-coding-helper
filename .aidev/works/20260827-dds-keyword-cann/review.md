# レビュー: CAnn / CFnn の取りこぼしを直す

## ラウンド 1（自己レビュー）

対象: `git diff main`（生成側 2 本・検査 1 本・詳細ページ 4 件・再生成データ 2 件・README）。

### must / should / nit

なし。

### 観点ごとの確認

- **要件適合**: AC1-AC5 すべて `test.md` に確認あり。特に AC4（修正前のデータで落ちる）は
  検査の強化が本当に効いているかを示すもので、実際に走らせて確認した。
- **価値適合**: ゴールは「原典の索引にある名前が 1 件も欠けていない状態」＋
  「次に同じ落ち方をしたら止まる状態」。前者は再生成で、後者は名前の突き合わせで満たしている。
- **正確性**:
  - 生成は **2 段**（索引 → 構文）で、索引側だけを走らせると `syntax` / `hasParameters` が消える。
    一度これを踏んだので、**両方走らせた結果**を突き合わせて「既存 174 件は変更 0」を確認した。
  - 検査の「名前の形」を `nn` 許容にし忘れると、`CAnn` が**不正な名前**として弾かれる。直してある。
  - 正規表現は生成側 2 本と検査側で**同じ形**にした（片方だけだと名前は出るが構文が付かない）。
- **規約適合（AGENTS.md）**: 原典照合は**主エージェントが実施**した。
  索引ページ（`docs/origin/dds/DSPF-KEYWORDS.html`）の生テキストから名前を機械的に取り出し、
  カタログとの差集合が `CAnn` / `CFnn` の 2 件であることを確認している。
  詳細ページの取得元 URL は `decisions.md` D2 に記録した（捏造していない）。
- **保守性**: 名前の取り出しが 3 か所に散っているのは元からの形。
  今回は**同じ形に揃える**に留めた（共有モジュール化は原典スクリプト全体の作りに関わるので、
  この work の範囲を超える）。コメントで「同じ形にしておくこと」を明記した。

## 判定

must 0 / should 0 / nit 0。差分は小さく（データの再生成を除けば 3 ファイル）、
処理フローも単純なので **walkthrough は挟まない**。
