# レビュー: 5250 の配色

## ラウンド 1（自己レビュー）

対象: `git diff main`（原典スクリプト 2 本 ＋ 生成物 ＋ core 2 本 ＋ UI ＋ 新規テスト ＋ 実機検証）。

### must

なし。

### should

**S1: 実装が原典の散文どおりで、実機と 13 件食い違っていた** — 実機で確定させた。

これは**手順で防いだ**もの——plan で「実機との突き合わせを UI より先に置く」と決めていた。
最初の実装は原典の「無視されます」の散文を素直に写しており、実機に流したところ 13 件が違った。
そこから 3 点（桁区切り線をモデルに入れない / `COLOR`＋`RI`＋`UL` は `UL` が落ちる /
非表示なら他の属性を立てない）を直して **61/61 一致**にした。
経緯は `decisions.md` D1・D2 に残した。

### nit

- **N1**: 吹き出しに `**非表示**` と強調記号を書いていた（`title` は素のテキストなので記号が出る）
  → 直した。
- **N2**: 生成スクリプトが脚注番号を落とす処理をセル全体に掛けていて、
  **16 進値（`20`）まで消していた** → 語ごとに落とすようにした。

## 観点ごとの確認

### 要件適合

AC1-AC7 と AC-I1〜I5 すべて `test.md` に確認あり。
**AC6（原典から生成し検査で固定）**は `npm run verify` の 15 項目目として入っている。

### 価値適合

ゴールは「**キャンバスが実機と同じ配色で出ている状態**」と
「**書いたのに出ない組み合わせが画面で分かる状態**」。
後者は `DSPATR(UL HI RI)` を入れると項目が点線の枠だけになり、吹き出しに理由が出る
——**コンパイルも通り警告も出ない**組み合わせで、いままでは実機に出して初めて分かった。

### 正確性

- **表を手で書いていない**（AGENTS.md）。原典の 2 つの表から生成し、
  検査が**2 表の一致**まで見る。
- **散文の規則を実装していない**（`decisions.md` D3）。ビットで表すと自然に出る。
- 原典と実機の食い違いは**実機を採り**、根拠をコメントと `decisions.md` に残した。
- 単体は「DSPATR の全 32 通りが 32 個の別のバイトになる」まで見ている（潰れの検出）。

### 規約適合（AGENTS.md）

- **原典照合は主エージェントが実施**した。HTML の表を `<tr>`/`<td>` で構造のまま読み、
  平文化した文字列からは起こしていない（表は平文化すると列が消える）。
- **原典から機械的に決まる成果物はスクリプトで生成**した。JSON を手で直していない。
- **実機で確かめた**うえで、確かめた範囲（61 通り）と確かめていないもの
  （明滅の見た目・桁区切り線・条件つきの `COLOR`）を `test.md` に明記した。

### 保守性

- core が持つのは**表の引き方だけ**（`dspfAttributes.ts` は 150 行ほど）。
- 色の値は利用者のエミュレータと同じにし、出所をコメントに書いた。

## 判定

must 0 / should 1（対応済み）/ nit 2（対応済み）。差し戻しなし。
原典・実機・core・UI にまたがり、判断の根拠が実測なので **walkthrough を挟む**。
