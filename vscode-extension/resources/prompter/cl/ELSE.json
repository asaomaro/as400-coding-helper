{
  "keyword": "ELSE",
  "description": "ELSE（ELSE）",
  "help": "対応する IF コマンドの論理式評価が偽の場合に処理されるコマンドを指定します。各 ELSE は直前の最も内側の IF と対応します。",
  "parameters": [
    {
      "name": "CMD",
      "description": "コマンド",
      "help": "対応する IF の式評価が偽の場合に処理する1つ以上のコマンド（DOグループ内で）を指定します。指定しない場合、偽条件では処置は取られません。",
      "inputType": "text",
      "required": false,
      "placeholder": "GOTO Y"
    }
  ]
}
